# T27 — claim_verifier + фиксы T1-экстракции (Спринт 1 SEARCH2, без LLM)

Приоритет: **P1**. Findings: L01 + подтверждённые находки аудита 2026-09-06. Статус: BACKLOG.
Спринт 1 дизайна `ai-team/reference/SEARCH2_DESIGN.md` §3.1–3.2, §7. Не требует LLM-ключей.

## Зависимости

Нет (чистый домен + extraction; с T28 не пересекается по файлам — можно параллельно).

## Обязательные отдельные вызовы

- ashyq-planner
- ashyq-developer
- ashyq-qa
- ashyq-reviewer
- ashyq-security

## Резервы (координационные)

- `extraction`
- `domain-contract`

## Начальный допустимый scope

- `backend/app/domain/claim_verifier.py` (новый)
- `backend/app/adapters/extraction.py`
- `backend/app/adapters/requirements/web_requirements.py` (только ветка fee-waiver)
- `backend/tests/test_claim_verifier.py` (новый), `backend/tests/test_live_regressions.py`
- `backend/tests/fixtures/live_shapes/**`

Planner подтверждает scope на HEAD; расширение — через диспетчера. QA пишет только тесты.

## Acceptance

- [ ] Верификатор: excerpt — дословная подстрока текста страницы после нормализации (NFKC,
      схлопывание пробелов); отклонение при любом несовпадении
- [ ] Верификатор: value соответствует формату/диапазону claim_type (IELTS 0–9 шаг 0.5;
      год ≥ текущего; сумма > 0) — таблица диапазонов в данных, не в коде
- [ ] Верификатор: source_url — суффикс-матч регистрируемого домена, НЕ подстрока URL
      (регрессия на `?u=narxoz.kz` из аудита)
- [ ] Верификатор: page_type допускает claim_type (текущая ACCEPTS-матрица)
- [ ] FEE_WAIVER_AVAILABLE: отрицания в строке дают False/unknown, не True
- [ ] `_MONEY`: 2-значные суммы рядом с валютой не становятся tuition (фикстура
      «$50 application fee»)
- [ ] Каждый реальный FP — новая фикстура в live_shapes; полный suite зелёный, coverage ≥ 92
- [ ] Верификатор — чистая функция без I/O; unit-покрытие 100%

## Выполнение

Прочитай `ai-team/TEAM_RULES.md`, `ai-team/reference/SEARCH2_DESIGN.md` §1, §3.2,
`SEARCH2_REVIEW_2026-09-07.md` и `INDEPENDENT_AUDIT_2026-09-06.md`. Contract → independent
regression (RED на текущем поведении для каждого FP) → implementation → QA → frozen-SHA
review → integration gate.
