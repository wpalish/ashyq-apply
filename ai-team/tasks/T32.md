# T32 — Freshness 2.0: SUPERSEDED, source_scan, reextract_page, refresh строки
(закрывает T13 и SourceChange-часть T26)

Приоритет: **P1**. Findings: L01, J01. Статус: BACKLOG. Дизайн `SEARCH2_DESIGN.md` §4–5, §7.
Суперсессия: **T13 закрывается этой задачей** (её подход «перечитать всё» заменён точечной
переэкстракцией; регресс-тесты T13 входят сюда).

## Зависимости

T28 (source_pages). С migrations сериален с T26/T28 — не писать параллельно с ними.

## Обязательные отдельные вызовы

- ashyq-planner
- ashyq-developer
- ashyq-qa
- ashyq-reviewer
- ashyq-security

## Резервы (координационные)

- `migrations`
- `runner`
- `jobs`
- `freshness`

## Начальный допустимый scope

- `backend/app/domain/freshness.py` (окна → Settings; next_recheck_at от source_pages)
- `backend/app/models/**`, `backend/migrations/versions/*.py` (SUPERSEDED, claims.source_page_id)
- `backend/app/jobs/source_scanner.py` (новый, по образцу payment_reconcile), `jobs/store.py` (kind)
- `backend/app/api/routes_results.py` (POST /runs/{id}/results/{rid}/refresh, tenancy)
- `backend/tests/test_freshness*.py`, `test_worker.py`

## Acceptance

- [ ] Миграция: ClaimStatus += SUPERSEDED; claims.source_page_id nullable; один head;
      апгрейд/даунгрейд на SQLite и PostgreSQL
- [ ] reextract_page: старые claims страницы → SUPERSEDED, новые рядом; история
      «было X (дата) → стало Y (дата)» запрашиваема
- [ ] SUPERSEDED-claims НЕ участвуют в find_conflicts и не дублируют evidence (регрессия
      на симптом дублей из аудита runner)
- [ ] source_scan: суточно на вуз, sitemap lastmod → условный GET → очередь переэкстракции
      только изменившихся; Crawl-delay/robots уважены; джоба в durable queue
- [ ] Регресс-тесты T13 (off-by-one freshness + коллизия idempotency-ключа) включены и зелёные
- [ ] refresh строки: tenancy-проверка как у соседних эндпоинтов; 402 на free-тире
- [ ] Прогон абитуриента читает базу claims; demo-прогон не меняется байт-в-байт

## Выполнение

Прочитай `ai-team/TEAM_RULES.md`, `SEARCH2_DESIGN.md` §4–5, `SEARCH2_REVIEW_2026-09-07.md`
(пробелы 3, 4). Contract → independent regression (RED тесты T13 сначала) → implementation →
QA (две PG-сессии для гонок джоб) → frozen-SHA review → integration gate.
