# T28 — source_pages + условные GET + PII на redirect-хопах (Спринт 1 SEARCH2)

Приоритет: **P1**. Findings: L01 + находки аудита. Статус: BACKLOG.
Дизайн `SEARCH2_DESIGN.md` §4.1–4.2, §7. Фундамент freshness-контура и walker'а.

## Зависимости

T16 (egress/browser hardening) — жёсткая: fetching.py сериализуется, два пишущих в него запрещены.

## Обязательные отдельные вызовы

- ashyq-planner
- ashyq-developer
- ashyq-qa
- ashyq-reviewer
- ashyq-security

## Резервы (координационные)

- `fetching`
- `models`
- `migrations`

## Начальный допустимый scope

- `backend/app/adapters/fetching.py`
- `backend/app/models/**` (SourcePage)
- `backend/migrations/versions/*.py`
- `backend/tests/test_fetch*.py`, `backend/tests/test_source_pages.py` (новый)

После T28 любые изменения fetching.py/models/migrations снова сериализуются (T29/T32 ждут).
Planner подтверждает scope; QA пишет только тесты.

## Acceptance

- [ ] Миграция `source_pages` (url, registrable_domain, institution_key, etag,
      last_modified_header, content_hash, lastmod_seen, fetched_at, http_status, page_type,
      active_claims) — единственный новый head alembic
- [ ] Fetcher сохраняет ETag/Last-Modified и content_hash (sha256 нормализованного текста);
      условный GET `If-None-Match` → 304 = ноль переэкстракции
- [ ] `find_pii` вызывается на КАЖДОМ redirect-хопе (регрессия из аудита; тест с
      PII-параметром на хопе)
- [ ] Политeness не тронута: robots/Crawl-delay/лимит 2 на хост работают как раньше
- [ ] Retention-политика для source_pages зафиксирована (что чистим и когда) — ResponseCache
      не должен повторить неограниченный рост
- [ ] Две PG-сессии: конкурентная запись одного url не создаёт дубль source_page
- [ ] Апгрейд/даунгрейд миграции проверены на SQLite и PostgreSQL

## Выполнение

Прочитай `ai-team/TEAM_RULES.md`, `SEARCH2_DESIGN.md` §4–5, `SEARCH2_REVIEW_2026-09-07.md`
(пробелы 1–4). Contract → independent regression → implementation → QA → frozen-SHA review →
integration gate.
