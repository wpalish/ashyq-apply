# T26 — News-слой (сужено; SourceChange/SUPERSEDED перенесён в T32)

Приоритет: **P1**. Findings: L01. Статус: **BLOCKED_SCOPE_CONTRACT** (2026-09-08).
SourceChange-контур закрыт T32, но текущий scope не разрешает ingestion/API/frontend,
необходимые для обещанного пользователю News-слоя. См.
`ai-team/outputs/c2-t26-a0/contract-audit.md`.

## Зависимости

T28 (source_pages), T32 (SUPERSEDED-миграция и точечная переэкстракция).

## Обязательные отдельные вызовы

- ashyq-planner
- ashyq-developer
- ashyq-qa
- ashyq-reviewer
- ashyq-security

## Резервы (координационные)

- `news-models`
- `migrations`
- `runner`

## Начальный допустимый scope

- `backend/app/models/**`
- `backend/app/domain/**`
- `backend/app/pipeline/runner.py`
- `backend/migrations/versions/*.py`
- `backend/tests/test_news*.py`

Planner должен подтвердить scope на текущем HEAD и **согласовать миграцию с T32** (serial:
migrations — общий ресурс, параллельно с T32 не писать). QA пишет только тестовую часть scope.

## Acceptance

- [ ] NewsEvent датирован, несёт источник и окно применимости; не смешивается с claims
- [ ] Событие не меняет материальные значения без верификатора (тот же контракт §1 SEARCH2)
- [ ] Показ в UI отделён от фактов (регистр «мнение/событие», не claim)
- [ ] Freshness-интеграция: событие уважает окна и SUPERSEDED-историю из T32
- [ ] Legacy-данные безопасны: миграция только добавляет, апгрейд/даунгрейд проверены

## Выполнение

Прочитай `ai-team/TEAM_RULES.md`, `ai-team/reference/SEARCH2_DESIGN.md` (§4) и
`ai-team/reference/SEARCH2_REVIEW_2026-09-07.md`. Аудит относится к f018307; проверь
актуальность. Contract → independent regression → implementation → QA → frozen-SHA review →
integration gate. Не выдавай артефакт одного агента за четыре независимых проверки.

До смены статуса владелец/продукт должен выбрать: расширить scope до полноценного
вертикального slice (рекомендуется) либо явно переименовать и сузить acceptance до
storage-only foundation. Пустая таблица `NewsEvent` не считается выполненным News-слоем.
