# T29 — catalog_walker + JS-каталоги (Спринт 1 SEARCH2, главный удар по recall 1/10)

Приоритет: **P1**. Findings: L01. Статус: BACKLOG. Дизайн `SEARCH2_DESIGN.md` §2.2–2.3, §7.

## Зависимости

T16 (browser hardening — walker-тир и перехват JSON идут только по hardened-тиру),
T28 (найденные программные URL сохраняются в source_pages).

## Обязательные отдельные вызовы

- ashyq-planner
- ashyq-developer
- ashyq-qa
- ashyq-reviewer
- ashyq-security

## Резервы (координационные)

- `discovery`
- `network-adapters` (чтение browser.py допустимо только после T16; записи — нет)

## Начальный допустимый scope

- `backend/app/adapters/discovery/catalog_walker.py` (новый)
- `backend/app/adapters/discovery/live_discovery.py` (только интеграция ступени walker)
- `backend/app/adapters/page_classifier.py` (дообучение PROGRAM_CATALOG, enum-решение)
- `backend/tests/test_live_discovery.py`, `backend/tests/fixtures/live_shapes/**` (+3 JS-каталога)

## Acceptance

- [ ] PROGRAM_CATALOG → walker извлекает ссылки-кандидаты (признаки «программности» по
      окружению, top-N = verify_limit, без выхода за тот же домен)
- [ ] Каждая ссылка идёт через Fetcher (robots/PII/politeness); исход не PAGE_FOUND —
      в page_outcomes с причиной
- [ ] Профильный фильтр (степень/поле/язык) переиспользует предикат фикса 02d648c
- [ ] JS-каталоги: ожидание сетевого покоя + перехват JSON-ответа с массивом {name,url}
      через hardened browser-тир; 3 JS-фикстуры покрывают путь офлайн
- [ ] Найденные программные URL пишутся в source_pages (T28)
- [ ] Офлайн-фикстуры: полный прогон demo не меняется байт-в-байт без walker-ступени
- [ ] Harness замера program recall готов (сам замер на живых вузах — T30, не здесь)

## Выполнение

Прочитай `ai-team/TEAM_RULES.md`, `SEARCH2_DESIGN.md` §2, `SEARCH2_REVIEW_2026-09-07.md`
(пробелы 1, 2). Contract → independent regression → implementation → QA → frozen-SHA review
→ integration gate. Начинать только после DONE T16.
