# T29 A1 (+A2 amendment) — Reviews (both on frozen candidate ff6a90e3330c6040bfce02c63c6f8c163398d914)

## Reviewer (agent_5834a789-8c92-4bf9-970d-4863ef39fe11) — VERDICT: PASS

ACCEPTANCE_MAP 7/7: (1) walker top-N same-domain links — extract_links :164-203 with same_institution gate :186, score_link :252-289, sort (-score,len(url)) :438; trigger PROGRAM_PAGE < MAX_PAGES_PER_CATEGORY(3), top-2 catalogues live_discovery.py:906-916; (2) every link via Fetcher with outcomes+reasons — _read_lead fetch only via fetcher.get :443, outcome per link incl. walker_budget_exhausted :413-415; (3) 02d648c reused — profile_rejects :461-477 line-by-line identical to baseline :802-819, shared by _confirm_programs :870 and walker :455; (4) JS tier at adapter level — response_listener :139 capped 20×1MB :40-82, bodies read while context open, failures swallowed; networkidle settle w/ fallback :210-218; all T16 gates byte-identical vs baseline (full-file compare); CatalogRenderer :306-336; parse_catalog_json depth≤4/entries≤200 never raises :206-249; 3 fixtures exercised; (5) source_pages recording MET AT ADAPTER LEVEL, DORMANT in production (R2 deferred): CatalogWalker._record :502-531 exactly 9 frozen kwargs matching SourcePage.record; recorder=None + status_code=None gates; runner.py:377/:859 construct bare adapter, no CatalogRenderer construction in app/ — CONDITION: item 5 not production-complete until post-T32 wiring; T30 must not run before it; (6) demo byte-identical — fixture_discovery/runner untouched, walker only in live adapter; (7) harness ready — trace.walker TypedDict :490-505 + as_dict "walker" :561 + walker_outcomes :743/:951-953.

Deviations APPROVED: (a) homepage-as-last-root gated on used_navigation_fallback + empty catalogue (required by R10 + pre-existing :852 pin — tests authoritative); (b) walker confirm = profile_rejects(page, degree, []) — R5 requires JSON "BSc Mathematics" to confirm for CS applicant; field_mismatch unreachable in A1, vocabulary kept for T30; (c) catalog_walker→app.adapters.browser import accepted ("no browser import" = no playwright/launch in pure logic; graph acyclic, lazy import one-way); (d) CatalogRenderer stale-payload clear-in-place fix correct (:333, snapshot at :489).

Nonblocking: js_no_program_list duplicated when catalogue fetch itself failed (:384-407); _NOT_CATALOGUE_TYPES annotation only on HTTP path (:482-489 vs :494-499); real-Chromium listener-vs-context-close race (dormant at A1 — validate at wiring/T30); AttributeError arm in settle except masks genuine attribute errors on real Page (:217); duplicate off-domain drop entries (:186-189).

Test adequacy: R1-R10 behavioral, RED log genuine (10 failed/114 passed), counts consistent (112+2 green pins=114; +10=124); amendment annotation-only verified line-exact; T16 negative controls present.

## Security (agent_898c1190-250b-47e8-b7a0-ff7aaa654119) — VERDICT: PASS

(1) JS interception CONTAINED — listener reads only route-gated page subresource bodies, in-process, capped 20×1MB, failures swallowed; NO listener registered in A1 production (runner.py:290-294 plain BrowserFetcher); cross-origin JSON links must clear same_institution AFTER parse (:396-398) — applies identically to JSON and HTML links; no exfiltration channel (URLs only become polite GET targets on the institution's registrable domain). (2) Walker amplification BOUNDED AND RECORDED — ≤40 same-domain fetches/institution (vs prior 8+5), every fetch rides Fetcher (robots :805, semaphore 2 :56/:804, spacing :683, hop PII :514, hop re-pin :471, 5MB cap :593, content-type :564); worst case ~40 polite serialized renders/institution — LOW self-directed latency. (3) parse_catalog_json SAFE with one crack — non-http(s) dropped :241, scheme-relative resolved :239, javascript:/data: triple-gated (parse scheme + same_institution hostname + network_policy scheme allowlist :117-120 incl. credentials); S-29-01 LOW: RecursionError escapes "never raises" on deep-nested JSON (except TypeError/ValueError :218), contained one frame up by walk() except Exception :363, dormant in A1. (4) Recording dormancy CONFIRMED — keyword-only None default, _record early-return :515, no production construction. (5) networkidle stall LOW/BOUNDED — +≤8s once then fallback; exception surface covered (exotic → render-level except :220 → TIMEOUT → HTTP fallback :490; walker failures swallowed :363-369/:929-931; CancelledError correctly not swallowed). (6) HTML parsing bounded — MAX_LINKS_SCANNED=400 :182, labels ≤160 :194, flat regexes (no ReDoS). (7) Weakening CLEAN.

NEW FINDINGS: S-29-01 LOW RecursionError (fix at wiring attempt); S-29-02 LOW JSON label length unbounded vs 160-char HTML cap — trace/report bloat only (parity truncation before T30); S-29-03 INFO record render counts + wall-clock in T30 canary (~40 renders/institution ceiling).

Guards not weakened: fetching/runner/models/migrations/config/domain/page_classifier absent from whole-chain diff; T16 controls verified present (PII assert :141, pre-check :143-146, robots gate :161-164, hardening :176-183, route gate :188, dialog :198, status mapping :230-245); T28 hop guards intact; no new FetchOutcome; walker_outcomes not read by runner in A1; demo byte-identical.

Required extra tests: deep-nesting fixture (pins S-29-01); 1MB-name truncation (pins S-29-02); unicode/scheme-relative/credentials URL drops; PlaywrightError-after-sleep settle; 400 off-domain anchors → zero fetches; robots-disallowed leads → no rows when wired; live-Chromium smoke at R2 wiring (both reviews flag).

RESIDUAL: R2 wiring attempt needs its own review (recorder swallow-and-log, CatalogRenderer construction, listener timing); T30 gated on wiring + live smoke.
