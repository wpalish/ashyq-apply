# T29 A1 — Planner contract (frozen)

- Invocation ref: agent_035586ea-880e-47db-905b-22c10e074d00
- Baseline: 2ed4f51e1d5c1fae3f4cb36ad64982eb67184a2d (ai/c2/integration = T16+T28+T27)
- STATUS: PLANNED
- DISPATCHER RULINGS on planner's requests (2026-09-07):
  - R1 browser.py — APPROVED: additive `response_listener` kwarg + networkidle settle; T16 controls byte-identical; negative controls pinned in test_browser_network.py stay green.
  - R2 runner.py — DEFERRED: `runner` lock is exclusively T32's this flight (its runner.py edits are the J01 fix core). The two T29 seams (CatalogRenderer construction ~:290; page_recorder pass ~:377) become a dispatcher-tracked micro-wiring attempt immediately after T32 lands. In A1: `page_recorder` param defaults None (dormant), CatalogRenderer exists in catalog_walker.py, offline-tested via _browser seam; production activation = wiring attempt. Acceptance items 4-5: implemented+tested at adapter level; production wiring tracked separately — T30 cannot run before wiring lands.
  - D1 verify_limit: WALKER_TOP_N=20 module constant (= current default); settings change deferred until T30 canary data.

## Facts (c2-integration worktree)

- Discovery flow: _discover_one live_discovery.py:686-777 (seeds → sitemaps → _navigation_fallback walking selected[PROGRAM_CATALOG][:2] + homepage, MAX_FALLBACK_PAGES=5 :78/862-947 → _confirm_programs :779-824 fetches ≤MAX_PROGRAM_CANDIDATES_CHECKED=8 (:74), classify_page, keeps PROGRAM_DETAIL/INTAKE_SPECIFIC_PROGRAM).
- 02d648c predicate live_discovery.py:802-816 (degree_level != profile level → reject; fields non-empty and not matches_field_text → reject; matches_field_text :419-434). URL helpers: matches_field :375, matches_degree :437, names_other_degree_level :445.
- Same-domain: registrable_domain :251-275, same_institution :278-284, canonical_url :298-328, _URL_EXCLUSIONS :234-240, looks_like_catalogue :411-416. MAX_PAGES_PER_CATEGORY=3 :68; _apply caps programs at 3 :826-860.
- PageOutcome frozen dataclass base.py:51-65 (url, category, page_type, detail, readable_chars); problem categories runner.py:63; discovery today emits DiscoveryTrace only (:466-509, as_dict :494).
- catalog_walker.py ABSENT (verified).
- Classifier: PageType.PROGRAM_CATALOG page_classifier.py:27; catalog detection :341-348 (plural heading _PLURAL_PROGRAM_HEADING :109-114, _CATALOG :102-106, program_links>=5); titled JS shell classifies via _identity/title fallback :471-477; gaps: "Course finder" titles, brand-only shells → UNKNOWN.
- Fetcher/browser post-T16/T28: FetchResult validators (fetching.py:104-133); hop guards :456-533; 304→CACHED :541-550; robots+politeness :803-812; _maybe_render :622-655 calls self._renderer.render(result.url) single-positional (subclass-friendly); error renders never cached :636-642. browser.py: robots gate :101-105, route gate :129, dialog :139, fixed settle wait_for_timeout(1200) :144, status-mapped outcomes :156-171; NO page.on("response") yet.
- Offline JS seam proven: test_browser_network.py:151-162 inject renderer._browser = FakeBrowser(page); FakePage.on records handlers :97-119.
- source_pages: SourcePage.record source_page.py:76-145 (upsert; fetch-metadata overwrite, institution_key/lastmod_seen coalesce, active_claims untouched; flush no commit); current callers = tests only; NO adapter imports app.models today (grep).
- Pipeline/demo: runner.py:111 self.demo = run.demo_mode; :377 FixtureDiscoveryAdapter if demo else LiveDiscoveryAdapter; renderer disabled in demo :290-294; demo corpus fixture:// (fetching.py:691-733), no JS catalogs there.
- verify_limit default 20 (config.py:44), per-run override, capped by candidate_limit (runner.py:127); config.py OUT of scope.
- QA harness: StubSite.install test_live_discovery.py:46-85 (validator-aware fake_get post-amendment); registry via temp file :656-658; fixtures/live_shapes/ has 12 files incl. program_catalog.html; canary_discovery.py embeds discovery_trace :492 (T30 extends it, T29 must not).

## Frozen contract (essentials)

- NEW backend/app/adapters/discovery/catalog_walker.py: pure discovery logic (bs4 + live_discovery helpers + page_classifier; NO app.models, NO DB, NO browser import). Surface: WALKER_TOP_N=20, WALKER_MAX_CATALOGS=2 (:882 parity), WalkerLink(url,label,score,source), extract_links(html,base_url,domain) reusing _harvest_links semantics, parse_catalog_json (pure, depth≤4, {name|title,url} arrays; [] if none → HTML fallback; never raises), score_link (None = hard reject), walk_catalog(async, one catalogue page).
- Seam: live_discovery._discover_one after _confirm_programs (between :773 and :775); trigger ONLY when confirmed < MAX_PAGES_PER_CATEGORY (3), top WALKER_MAX_CATALOGS catalogues; walker-confirmed appended to selected[PROGRAM_PAGE] deduped; full list to trace. Trace additive: trace.walker = {catalogs_walked, candidates(capped 200), programs_confirmed, outcomes(capped 200)}; as_dict gains "walker" (T30 reads this). adapter.walker_outcomes: list[PageOutcome] category="catalog-walker", reset beside self.traces :678.
- PageRecorder = keyword Callable | None; LiveDiscoveryAdapter.__init__(..., *, page_recorder=None); default None byte-identical; runner wiring DEFERRED (R2).
- Scoring DATA-in-module: DEGREE_MARKERS regex tuples (bachelor/master/phd/foundation), SUBJECT_DEGREE pattern, MIN_LABEL_CHARS=10, REPEATING_LIST_MIN_SIBLINGS=5. Score: +5 degree marker, +4 SUBJECT_DEGREE, +2 repeating structure; reuse live_discovery.matches_field_text (dominates), matches_degree (+4/−6), names_other_degree_level → hard reject, _URL_EXCLUSIONS → hard reject. Same-domain via live_discovery.same_institution (planner deviation from claim_verifier.url_matches_domains: one crawl-boundary impl per module; divergence guard keeps suffix tables in lockstep; claim_verifier stays claim-side authority). Sort (-score, len(url)) :751 parity; top WALKER_TOP_N fetched.
- profile_rejects(page, degree, fields) -> str|None: factor :802-816 into one function used by _confirm_programs AND walker (behavior-preserving; existing tests green unmodified).
- Outcome vocabulary: fetch layer reuses FetchOutcome.value verbatim; walker layer frozen strings: program_detail, intake_specific_program, not_program_catalog, reads_as_<page_type>, degree_level_mismatch, field_mismatch, off_domain, short_label, excluded_url, walker_budget_exhausted, js_no_program_list.
- JS tier (R1 APPROVED): browser.py additive render(self, url, *, response_listener=None) — called with (response_url, content_type, body_text) for json content-types/.json URLs, cap 20 responses/1MB each, reads inside try before context.close(), listener failures swallowed; settle: try page.wait_for_load_state("networkidle", timeout=8000) except PlaywrightError → keep wait_for_timeout(1200). Route/robots/PII/status/dialog gates byte-identical. Invariant: interception only on pages whose navigation passed the robots gate. catalog_walker.CatalogRenderer(BrowserFetcher) overrides render() to pass self._collect; parse_catalog_json over stashed bodies; HTML+JSON merged, dedup canonical, JSON wins ties; JSON present but no array → HTML fallback; no renderer → HTTP HTML only. Offline: 3 fixtures in live_shapes (js_catalog_payload.json, js_catalog_shell.html, js_catalog_unrelated.json) + StubSite + FakeBrowser/FakePage local doubles (do NOT import from test_browser_network.py).
- source_pages recording (dormant until wiring): recorder called for every walker-fetched link with status_code not None (OK + definitive HTTP errors; robots/PII/timeout refusals — no row). Exact record kwargs frozen: url=canonical(final_url or url), registrable_domain=live_discovery.registrable_domain(hostname), etag/last_modified/content_hash from result or None, http_status=result.status_code, page_type=classification.page_type.value, institution_key=None, fetched_at=result.fetched_at. Recording failure must never fail discovery (runner-side swallow+log at wiring time).
- allowed_paths: catalog_walker.py NEW; live_discovery.py; page_classifier.py (catalogue-title/shell signals only; NO new PageType member); test_live_discovery.py; fixtures/live_shapes/** (+3). [R1 browser.py authorized; R2 runner.py deferred.]
- Non-goals: no live requests; no freshness/SUPERSEDED/source_scan (T32); no LLM; no weakening robots/politeness/PII/SSRF/route-gate/status-mapping; no registry expansion (T30); no demo corpus changes; no canary_discovery.py/fetching.py/models/migrations/domain changes; no new PageType; no architecture beyond one walker module + one recorder callback.
- Migration impact: none. Consumers: T30 (trace.walker + walker_outcomes; extends canary itself), T32 (rows feed lastmod/content-hash logic; institution_key=None coalesce matters).

## QA scenarios (named)

R1 walker_extracts_scored_links (top-3 by frozen scoring; short/excluded links never fetched) [ImportError RED]; R2 each_link_fetched_with_outcome (404→http_error; news→reads_as_news; programme→confirmed; trace.walker.outcomes + walker_outcomes rows); R3 profile_predicate_reused (MSc link for bachelor → degree_level_mismatch; label text dominates opaque URL); R4 off_domain_never_fetched; R5 js_json_payload_yields_programs (FakePage + {name,url} array; JSON link need not match URL patterns); R6 js_unrelated_json_falls_through (HTML fallback drives; nothing invented); R7 js_silent_shell_is_recorded_not_fatal (js_no_program_list, no crash); R8 source_pages_rows_recorded (recorder fake receives canonical url/domain/page_type/status/validators; institution_key None; migrated-SQLite pattern test_source_pages.py:37-54); R9 top_n_budget (30 candidates → exactly WALKER_TOP_N fetched); R10 no_catalog_byte_identical + demo_invariant (walker inactive without catalogue; demo never constructs walker/recorder).
Justified-GREEN pinning where baseline already behaves (titled JS shell classifying PROGRAM_CATALOG) — mark explicitly per T27 pattern.
