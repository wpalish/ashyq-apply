# T35 A1 — Planner contract (frozen)

- Invocation ref: agent_549d9b71-13e6-4c8d-8157-e30d287d7140
- Baseline: edf546de9f998c0909337d0abe6f8e1c9c4a0be5
- STATUS: PLANNED
- Dispatcher rulings: коррекции мастер-промпта приняты по evidence (legacy hash n=2**12 keyword-only, НЕ n=2**14; префикс f"scrypt${2**settings.password_scrypt_log2}$" = scrypt$16384$ в auth_client, НЕ scrypt$131072$; ValueError-guard для коротких legacy-паролей обязателен; actor="system" по семейству password_reset_*). config.py: параллельная разработка с T34 разрешена (хунки заякорены), интеграция T34 → T35 → T36.

## Facts (baseline, file:line)

- S4 CONFIRMED: mail.py:64 `smtp.starttls()` без context. Доказано против интерпретатора venv (CPython 3.12.13): smtplib.py:777-778 context None → ssl._create_stdlib_context() = _create_unverified_context (CERT_NONE, check_hostname=False) → STARTTLS шифрован, но НЕ аутентифицирован → MITM читает reset-токены. ConsoleSender :41-50 / RecordingSender :70-83 / get_sender :86-87 не трогать; тесты биндят RecordingSender на seam routes_account.get_sender. Коллизий настроек нет (grep smtp_tls_verify/CERT_NONE чист).
- S5 CONFIRMED: security.py:62-68 needs_rehash — единственная ссылка в app/ = определение; routes_auth.py login: verify_password :158 → membership :160-167 → create_session :168; needs_rehash не импортирован. hash_password(password, *, n=None) — keyword-only n РЕАЛЬНЫЙ (format scrypt$n$8$1$salt$digest), enforced 12 ≤ len ≤ 128. auth_client fixture ставит password_scrypt_log2=14 и патчит get_settings в 4 модулях → новые хеши scrypt$16384$; needs_rehash(16384)==False. Production cost 2**17 (config.py:82, floor :204-205). AuditEvent models/research.py:193-213 (organization_id NOT NULL FK, actor String(20), action String(60) indexed, detail JSON); семейство паролей: password_changed (actor user:<id8>), password_reset_requested/completed (actor "system"). LoginIn.password min_length=1 → rehash должен guard-ить ValueError от hash_password (иначе legacy-короткий пароль → 500 вместо логина).

## Frozen contract

- SMTP (mail.py): import ssl; SmtpSender.send — ctx = ssl.create_default_context(); if not settings.smtp_tls_verify: ctx.check_hostname=False; ctx.verify_mode=ssl.CERT_NONE; smtp.starttls(context=ctx). Без других протокольных изменений (никакого SMTP_SSL/port logic).
- Settings (config.py, вставка после :96): `smtp_tls_verify: bool = True` + docstring-комментарий (env: UNIMATCH_SMTP_TLS_VERIFY).
- validate_runtime (config.py, вставка между :201 и :202 — сразу после smtp-host проверки): `if self.is_production and not self.smtp_tls_verify: raise RuntimeError("UNIMATCH_SMTP_TLS_VERIFY must be true in production; STARTTLS without certificate verification would deliver password-reset mail through anyone on the path.")` (в проде кодпуть достижим только при email_sender != "console" — та проверка стреляет раньше; лишний конъюнкт не нужен).
- Rehash (routes_auth.py): импорт needs_rehash (после hash_password, алфавитно) + AuditEvent в импорт моделей; вставка между :167 (403 membership) и :168 (create_session): `if needs_rehash(user.password_hash): try: user.password_hash = hash_password(payload.password) except ValueError: pass (комментарий) else: session.add(AuditEvent(organization_id=membership.organization_id, actor="system", action="password_rehashed", entity_type="user", entity_id=user.id, detail={})); session.commit()`. Позиция после membership-403 даёт валидный organization_id. Ровно один раз — состоянием (needs_rehash(new) False; strict <). Конкурентность: два одновременных логина могут дать до 2 строк — принято, безвредно.
- RED-1 test_starttls_gets_a_verifying_ssl_context (FakeSMTP monkeypatch на app.mail.smtplib.SMTP; baseline: kwargs context отсутствует; после: check_hostname True + CERT_REQUIRED). RED-2 ..._can_be_disabled_for_a_self_hosted_relay (Settings smtp_tls_verify=False → CERT_NONE; тоже RED на baseline). RED-3 test_login_upgrades_an_old_cost_hash_and_audits_it_once (auth_client; register; SessionLocal overwrite password_hash = hash_password(PASSWORD, n=2**12); login → 200; хеш изменился, префикс f"scrypt${2**log2}$"; needs_rehash False; ровно 1 AuditEvent password_rehashed по entity_id; второй логин → 0 новых). GREEN-guard: логин с неверным паролем на legacy-аккаунте → 401, хеш не меняется, 0 событий; + test_a_legacy_short_password_login_is_not_broken (ручной hashlib.scrypt хеш короткого пароля → 200, без 500/audit). GREEN: production + smtp_tls_verify=False → RuntimeError match "UNIMATCH_SMTP_TLS_VERIFY"; True → валидно.
- allowed_paths: mail.py (import ssl + send :57-67), config.py (ровно два хунка), routes_auth.py (импорты + вставка между :167/:168), тесты test_account_flows.py (test_logging_and_correlation.py в карте, ожидается неиспользуемым). security.py НЕ нужен (helpers корректны). Не в scope: routes_account, models, schemas, migrations, frontend, compose.
- Non-goals: без SMTP_SSL/port/timeout изменений; без ConsoleSender/RecordingSender/get_sender; без изменений reset-flow; без новых зависимостей; audit detail={} (richer detail — non-goal); без perf-теста (мастер-промпт отменяет).
