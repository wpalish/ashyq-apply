# T10 / A1 — reviewer + security outputs (сохранено диспетчером; сокращено без изменения оценки)

## ashyq-reviewer — INVOCATION_REF: agent_74a0e896-28dd-4950-8fff-9530765ddc79
### VERDICT: PASS (только на df1ad1194abcd7812eb9bf7d66e417b3d211c157)
- FINDINGS (все non-blocking): 1. LOW worker.py:128-131 — token перечитывается из store.get(job_id).attempts в execute(), не пробрасывается из claim_one (residual пробы 6; окно ещё уже: нужен stall >120s + переживание backoff ≥30s при живом loop). Follow-up: возвращать (job_id, attempts) из claim_one. 2. LOW (вне scope T10) worker.py:208-223 — payment_reconcile: reconcile_order записал apply_status до fence-проверки; fenced fail → session_scope коммитит бизнес-записи stale-попытки; derived-состояние, идемпотентно сходится, но кандидат на follow-up (ранний fence-check или rollback). 3. INFO — gates приняты по логам QA (read-only мандат).
- CONTRACT_COMPLIANCE: подтверждён построчно: attempts-токен (store.py:185, 153-157), без миграций (models/jobs.py без новых колонок, migrations/ чисты); все 7 update(Job) в store.py условные (grep); retry/dead SQL case — арифметика идентична backoff_for (45-47) и reap (409-417); LeaseLost ПЕРВЫМ в каскаде (runner.py:298-303); worker handler — только лог (136-141); TERMINAL финальны; attempts+1 ровно на claim; complete+audit+recheck одна транзакция (272-288).
- FENCE_COMPLETENESS: под fence — heartbeat (вкл. внутри _save), complete, fail, mark_cancelled, checkpoint-записи (renew-or-lose → rollback+LeaseLost), recheck-enqueue и job_completed audit (отбрасываются rollback'ом). Без fence — корректно: enqueue (производство), cancel (API-семантика), reap (take-away маркер), reconcile_startup (стартовая однопоточная). _save при job_id=None — только тестовая семантика (production инстанцирует runner только в _dispatch с job_id).
- ACCEPTANCE_COVERAGE: 1-6 покрыты (TestStaleWorkerRunState, TestFencedWritesTwoSessionPostgreSQL 783-985 с same-worker_id reclaim 783-836, state machine probes + test_jobs 30/30 PG, TestCheckpointKeepsPendingCounters, TestStaleDispatchCommit 996-1041).
- TEST_ASSESSMENT: QA-тела не изменены; 8 тестов ассертят состояние БД (не пустышки); слабое место: TestStaleDispatchCommit монкипатчит run_to_decision в no-op (сценарий сужен намеренно, полный mid-run путь покрыт другим классом).
- SCOPE: ровно 3 файла; публичный API JobStore совместим (lease_token kw-only опциональный; callers routes_research/payments/metrics не затронуты).
- MIGRATION_COMPATIBILITY: миграций нет; legacy-данные совместимы (fenced-предикаты не совпадут для не-владельца).
- UNTESTED: RunCancelled+LeaseLost одновременно; payment_reconcile на PG; окно re-read.
- NEXT: принять df1ad119; findings 1-2 → planner как follow-up; интегратор обязан повторить affected gates на combined SHA.

## ashyq-security — INVOCATION_REF: agent_b662c4b1-69fb-4886-874f-359490d99768
### VERDICT: PASS (только на df1ad119...)
- RACE_INVENTORY: полная таблица операций → защит → residuals (claim FOR UPDATE SKIP LOCKED; heartbeat/complete/fail/mark_cancelled — тройной предикат id+status=running+worker_id+attempts=token; cancel — условный UPDATE NOT IN terminal RETURNING; reap — take-away UPDATE RETURNING + outcome под маркером; _save — renew-or-lose в транзакции бизнес-записей; LeaseLost ПЕРВЫЙ; dispatch rollback при complete→False).
- FINDINGS: F-1 [non-blocking, known] token re-read window — уточнён и подтверждён. F-2 [non-blocking для T10, money-adjacent, уточнён ущерб] payment_reconcile: fenced fail не роллбэчит сессию → session_scope коммитит PaymentEvent-journal/order.status stale-попытки; (a) возможна регрессия paid→non-terminal до следующего poll (~30s); (b) apply_status(PAID) у stale-попытки → _grant_and_schedule создаёт НОВЫЙ ResearchRun безусловно (двойной run/research job; grant_case_access идемпотентен, run — нет). Внешний блокирующий вызов get_invoice при удерживаемом lease повышает likelihood (lease истекает естественно). Рекомендация: follow-up rollback при fenced fail + защита _grant_and_schedule. F-3 [new, low] reconcile_startup check-then-act (дубль recovery-записи, самокорректируется). F-4 [pre-existing, вне diff] routes_research.py:418,530 retry/recheck 409-проверка check-then-act → возможны два живых pipeline одного run (разные idempotency-ключи) → двойные дорогие side effects; run-level single-writer не enforced. Кандидат на отдельный finding.
- GUARDS: auth/tenant scoping не тронуты (Depends(get_principal) + owned_run по organization_id; collect — require_full_access); ResearchRunner инстанцируется только в worker.py:243; test_security.py присутствует, не в diff; новые PG-тесты — не пустышки.
- Token identity: attempts монотонный, атомарный инкремент в claim; token replay закрыт; DoS: backoff bounded 30/120/600, max_attempts=3 → dead, бесконечный retry невозможен; cancel не реанимирует terminal.
- REQUIRED_EXTRA_TESTS: fenced-fail commit proof (PG, две сессии — ожидание RED по F-2); дубль grant/run; токен из claim_one (после фикса); run-level параллельность; reconcile_startup race.
- NEXT: принять df1ad119; follow-ups (1) проброс токена из claim_one, (2) rollback при fenced fail в payment_reconcile + защита _grant_and_schedule (приоритет выше — money-adjacent); F-3/F-4 в backlog.
