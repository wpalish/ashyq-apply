# T32 A1 — Planner contract (frozen)

- Invocation ref: agent_c24335b5-c5bd-41f3-be24-a4fe75ea829b
- Baseline: 2ed4f51e1d5c1fae3f4cb36ad64982eb67184a2d (ai/c2/integration = T16+T28+T27)
- STATUS: PLANNED
- DISPATCHER RULINGS on planner's product decisions (2026-09-07): defaults stand — (1) FK SET NULL; (2) retention purge INCLUDED in T32 (T28 contract defers it here in writing); (3) demo refresh → 409; (4) is_stale boundary inclusive (expiry at exactly max_age; POSSIBLY_STALE display shifts <1 day — noted for owner).

## Facts (c2-integration worktree)

- J01 off-by-one VERIFIED LIVE: freshness.py:27-31 age_days = max(0,(now-accessed_at).days) whole-day floor; :34-35 is_stale = age_days > max_age_days → at TTL=30, age=30d+2min: 30>30 False → not stale; :47-50 next_recheck_at = accessed_at + window (already past at that moment).
- J01 chain-death VERIFIED: worker.py:179-196 _schedule_recheck idempotency_key=f"recheck:{run.id}:{when.date().isoformat()}" (date-granular); runner.py:242-260 recheck_stale no-op path: 0 stale → next_recheck_at = earliest_recheck() which recomputes from SAME immutable accessed_at (runner.py:219-240) → same past moment; worker.py:268-271 re-enqueues same date → same key → store.py:107-112 dedupe → zero new jobs → chain dead forever. Healthy path (runner.py:262-273 → :313) advances but collides on same calendar date. Matches INDEPENDENT_AUDIT_2026-09-06.md:15 verbatim.
- ClaimStatus: enums.py:46-52 StrEnum 6 members, no SUPERSEDED. claims.status String(40) (research.py:145-160), NO CHECK constraint anywhere → SUPERSEDED needs ZERO DDL. ClaimRow.payload embeds full Claim dict incl. status (runner.py:1074-1087); GET /claims returns **c.payload (routes_results.py:283-285) — status flip must update column AND payload. GET /claims already supports ?status= (routes_results.py:280-281).
- find_conflicts: conflicts.py:47-110 groups ALL input claims, no status filter; call sites runner.py:503, :609.
- Duplicate-evidence symptom: _replace_evidence runner.py:1061-1072 hard-DELETEs ClaimRow/ConflictRow by result_id — reextract history through this path would destroy было/стало.
- Jobs: models/jobs.py:42-83 kind String(40) (no enum), idempotency_key unique String(200), payload JSON; store.py:89-142 enqueue+dedupe+SAVEPOINT; :146-198 claim FOR UPDATE SKIP LOCKED, attempts=lease token; :200-317 fenced heartbeat/complete/fail/cancel; worker.py:198-298 _dispatch routes kinds, completion+work one transaction :278-288; existing kinds research/recheck/documents/payment_reconcile; NO cron infra — payment_reconcile enqueued per-order (payments/service.py:138-140); reconcile_startup worker.py:330-389 = bootstrap point.
- T28 handoff: SourcePage unique url, etag/last_modified/content_hash/lastmod_seen/fetched_at/http_status/page_type/active_claims; record() ON CONFLICT upsert, lastmod_seen COALESCEs (scanner must set lastmod_seen directly on returned instance :145); is_purgeable :148-170; docstrings say purge is T32's. Fetcher.get(etag=, if_modified_since=) fetching.py:740-776; 304 → CACHED status_code==304 (:537-548); robots/Crawl-delay inside Fetcher.
- Tenancy/402: api/tenancy.py:27-39 owned_run (404 cross-tenant); api/paywall.py:38-45 require_full_access raises PaymentRequired → payments/http.py:27-38 renders 402 code=payment_required (main.py:101); pattern used by list_claims routes_results.py:276. has_full_access payments/entitlements.py:17-36 (always True when payments disabled — 402 test needs payments_enabled=True).
- Tests: no test_freshness*.py yet; test_worker.py:468-561 TestFreshnessRecheck — test_a_recheck_with_nothing_stale... (:490) asserts success+no work but NOT re-enqueue (why chain-death is green); must NOT be weakened — add missing assertion as new RED test. PG patterns test_jobs.py:46-67, test_worker.py:783+ pg_factory. Demo: config.py:18 demo_mode=True; runner.py:111/377.
- Migration chain: head b4e8a1c2f6d9 (source_pages); T32 stacks on it; batch_alter_table style reference f13b9c5d2a44_run_next_recheck.py:21-34.

## Frozen contract (essentials)

- C1 Migration (one revision, down_revision="b4e8a1c2f6d9"): claims += source_page_id String(32) nullable + FK fk_claims_source_page → source_pages.id ON DELETE SET NULL (rationale: T32 purge may delete pages whose referencers are SUPERSEDED with active_claims=0; SET NULL keeps purge total, history stays queryable via payload.source_url) + ix_claims_source_page; research_runs += recheck_generation Integer NOT NULL server_default "0" (no backfill). ClaimStatus += SUPERSEDED "SUPERSEDED" — ZERO DDL, no CHECK constraint. Downgrade exact inverse. Round-trip SQLite+PG; exactly one head before/after.
- C2 reextract_page: kind "reextract_page"; payload {source_page_id, url, reason: sitemap_lastmod|content_hash|claim_stale|manual, run_id, result_id} (run/result REQUIRED — base claims out of scope); scanner enqueues one job per distinct (source_page_id, result_id) on live claims of that page. Fencing = T10 pattern. ONE TRANSACTION: flip old claims (status column AND payload["status"] → SUPERSEDED, set source_page_id, keep accessed_at/values) + insert new claims + SourcePage.record + lastmod_seen advance + job completion. Old superseded even when new extraction yields zero claims of that type (honest history-only; never keep VERIFIED_CURRENT the page no longer supports). Supersession scope: destination result's run, per source_url. _replace_evidence runner.py:1067 → delete only status != SUPERSEDED (keep lineage across retries); reextract never writes through _persist_result/_replace_evidence. find_conflicts entry filter drops SUPERSEDED before grouping (conflicts.py:47; covers :503/:609 + future callers); evidence payload builders exclude SUPERSEDED; apply_freshness cannot touch SUPERSEDED (pin with test). Idempotency: reextract:{source_page_id}:{result_id}:{generation}, generation = count of terminal reextract jobs for that prefix; NEVER a date.
- C3 source_scan: kind "source_scan"; payload {institution_key}; key source_scan:{institution_key}:{yyyy-mm-dd UTC} (date IS the generation here). Self-rescheduling durable job: bootstrap in reconcile_startup for each distinct institution_key in source_pages (no queued/running scan for today/tomorrow key → enqueue next UTC midnight + hash(institution_key)%3600 stagger); handler enqueues tomorrow's on success. No-ops in demo_mode. Flow (all network via Fetcher only): select rows for institution → robots.txt → sitemap (1-2 light requests) → per sitemap URL with stored row: lastmod > lastmod_seen or undecided → conditional GET fetcher.fetch(url, etag=row.etag, if_modified_since=row.last_modified_header) → 304: update fetched_at only, zero extraction; 200 + content_hash != stored → enqueue reextract_page per affected (page,result_id), else metadata-only → SourcePage.record then set lastmod_seen directly on returned instance. Sitemap w/o lastmod → fallback content-hash conditional GET of claim-bearing pages (active_claims>0). Pages with no live claims: metadata-only. ROBOTS_DISALLOWED → skip, record nothing. Two PG-session race tests: double-bootstrap no double-enqueue; lease takeover no duplicate reextract enqueues.
- C4 Retention purge INCLUDED: purge_source_pages(session, now) in jobs/source_scanner.py — bulk-delete rows where is_purgeable(...) (T28 predicate); once per day at end of last-completing source_scan or dedicated key source_scan:__purge__:{date} (one, no new infra); count+log only; FK SET NULL prevents purge failure on superseded-referenced pages; active_claims>0 never purged.
- C5 Refresh endpoint: POST /api/runs/{run_id}/results/{result_id}/refresh (routes_results.py, router prefix :35). require_full_access (404 cross-tenant, 402 free w/ standard body); result filter(run_id, id) → 404 like get_result. Demo run → 409 {"detail": "demo runs are read-only"}. 202 async {"status":"refresh_scheduled","pages":N,"job_ids":[...]}; resolves live claims' distinct source_urls → source_pages rows → enqueue reextract_page reason="manual" per (page,result_id) with generation keys; unknown URL → skipped_urls report; zero live claims → 202 pages:0 (valid no-op); no fetch/extraction in request loop.
- C6 было/стало: data model + existing API only — SUPERSEDED rows keep payload/accessed_at/run_id/result_id + source_page_id; queryable via GET /claims?status=SUPERSEDED joined on (result_id, claim_type, source_url); NO new endpoint in T32 (follow-up). Test proves old+new pair retrievable, ordered by accessed_at.
- C7 J01 fix: is_stale → (now_norm - accessed_norm) >= timedelta(days=max_age_days) (both UTC-normalized; age_days stays display-only). No-op recheck advancement: next_recheck_at = min over live claims of max(next_recheck_at(claim), now+1h) — strictly future. _schedule_recheck key → f"recheck:{run.id}:{run.recheck_generation}"; generation += 1 in same transaction as new next_recheck_at; completed generation keys never reused.
- C10 allowed_paths: freshness.py; enums.py (SUPERSEDED only); conflicts.py (entry filter); models/research.py (ClaimRow.source_page_id, ResearchRun.recheck_generation); migrations/versions/<one new>.py; jobs/source_scanner.py NEW; jobs/store.py (kind constant/doc only); jobs/worker.py (_dispatch branches, _schedule_recheck fix, bootstrap); pipeline/runner.py (recheck advancement, _replace_evidence filter) — serialized, T32 holds runner lock; api/routes_results.py (refresh only). Tests: test_freshness*.py NEW, test_worker.py, test_jobs.py, test_source_pages.py additions.
- C11 non-goals: no discovery/walker (T29); no news (T26); no LLM; no live network in tests; no nullable claims.run_id/base claims (§5.1 follow-up); no new history endpoint; no Fetcher internals changes; no retention policy change (execute only); no frontend; demo pipeline logic untouched.

## QA scenarios (C8)

R1 test_stale_at_exact_ttl_boundary (TTL−1s not stale; TTL exactly STALE [RED: freshness.py:35]; TTL+2min STALE; naive+aware variants); R2 test_noop_recheck_advances_the_chain (extends :490 without weakening: after no-op recheck a NEW recheck job exists, strictly-future available_at, different generation key [RED: zero new jobs]); R3 test_recheck_chain_survives_two_cycles; R4 test_superseded_excluded_from_conflicts_and_evidence (old X SUPERSEDED + new Y live → no conflicts, evidence only Y, old row survives with value); R5 test_reextract_supersedes_and_appends_atomically (PG two-session: forced page change → one commit; crashed/fenced attempt writes neither); R6 test_demo_run_is_byte_identical (payload JSON sort_keys golden + existing demo tests green); R7 test_refresh_endpoint (402 free w/ payments_enabled, 404 cross-tenant, 409 demo, 202+jobs paid, double-click one job set); R8 migration round-trip SQLite+PG. Plus unit: apply_freshness never touches SUPERSEDED. R1/R2 = true baseline-bug REDs; R3-R5/R7 = contract tests RED-pending-implementation (job kind/status don't exist); R6/R8 structural.
