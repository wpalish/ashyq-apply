# T32 A1 — Reviews (on frozen candidate 51a52c4b39381f873bdfabac8c3aaff2fbc02de3)

## Reviewer (agent_caa6a54d-b3ef-4acd-af5e-a8e21142e0e8) — VERDICT: PASS

ACCEPTANCE_MAP 7/7 (evidence in worktree c2-t32-qa-a3): (1) migration d9c4e7a21b83 FK SET NULL + ix + recheck_generation, exact-inverse downgrade, single head (own sweep of 16 version files), round-trip SQLite+PG + behavioral SET NULL tests :663-790; (2) reextract supersession source_scanner.py:250-296 (column AND payload flip, zero-yield supersedes), queryable via routes_results.py:343-370 + test :395; (3) conflicts entry filter conflicts.py:64 + _replace_evidence runner.py:1109-1112 keeps SUPERSEDED; R5 atomic/fenced :217-368; (4) source_scan: date-keyed slot + sha256 stagger :69-84, idempotent bootstrap :87-125, sitemap→conditional GET→reextract queue :394-494, 304 short-circuit :460-463, robots skip :404-409/:464-465, all network via Fetcher, self-reschedule in completion txn (worker :279-297), purge via is_purgeable only :512-536; demo guard BOTH entry points main.py:53 + worker.py:477, no third reconcile_startup caller (grep); (5) T13 fixed: inclusive >= freshness.py:55, generation key worker.py:217 (never a date), strictly-future arming runner.py:257-269 + worker clamp :199-203, R1-R3 green; (6) refresh routes_results.py:187-253 require_full_access → 404/402 ordering, demo 409 exact body, 202 payload, generation dedupe, DB+enqueue only; (7) demo golden unchanged hash aae8c595…d702 asserted :634; seed_demo untouched.

J01 FIX VERDICT: FIXED — no production date-granular recheck/reextract key (only source_scan/__purge__ where date IS the generation per C3); residual nonblocking: run.next_recheck_at column can hold past value with orphaned history, queue stays future via clamp.

Nonblocking followups (tickets; must NOT be fixed under this review): (1) recheck_stale stale-sweep :291-295/:1399-1406 has no SUPERSEDED filter → runs with history take the heavy path every recheck (chain survival unaffected); (2) multi-result supersession order hazard: URL backing live claims in ≥2 results of one run — wrong-order reextracts leave one result without live evidence (contract-conformant per C2 literal wording; recoverable via refresh); (3) worker :204-213 cancels ALL queued recheck siblings incl. user's manual recheck-now (routes_research.py:534-540) — silent drop, recheck happens later; (4) carried QA queue: generation-key race, 60s re-arm economics, refresh rate limit, sitemap partial-lastmod skip, purge O(n) load, arm_source_scans default-True future-caller note.

Amendments APPROVED: A2 facts-only (reflection keys fallback; _take_over real takeover — asserts RUNNING/attempts==2 mid-handler); A3 sound (frozen now/today/utcnow across 6 inventoried modules; golden NOT re-captured; masking deliberately not widened — 96 semantic corpus dates would have been hidden; negative control load-bearing).

Scope PASS (chain attribution per QA verify, consistent with tree reads).

## Security (agent_4968c997-0108-47ea-810c-d7960fa47ded) — VERDICT: CHANGES_REQUESTED

BLOCKING RC-1 (SEC-T32-01 MEDIUM): source_scanner.py:223 reextract fetch `await fetcher.get(url)` is cache-enabled validator-less → warm-cache read within TTL 86400 (fetching.py:778-783, config.py:38): manual refresh commonly re-reads OLD bytes → supersedes live claims + re-appends old content with accessed_at=now (claim dated today for an earlier read — violates evidence-traceability invariant) and was-v1→became-v1 churn; user's refresh silently no-ops until next daily scan self-heals (use_cache=False). Any tenant's earlier fetch can warm the shared cache (public content — staleness, not cross-tenant exposure). REQUIRED: fetch with the page's validators (stored etag/last_modified from source_pages), treat 304 as no-change completion (touch fetched_at, complete WITHOUT superseding — else every unchanged-page refresh dead-letters), or use_cache=False. Scoped to reextract_page + tests 1-2.

Attack scenarios clean: free-tier cannot reach enqueue (only routes_results:228 behind require_full_access :204 — 404 cross-tenant BEFORE 402 — and system scan :482; admin GET-only; demo 409; pinned :924/935/979/990); refresh cost amplification BOUNDED (generation dedupe + 1 polite GET per terminal cycle — LOW, rate-limit product follow-up); worker takeover/fencing SAFE (store.py untouched; attempts-token conditional writes; _complete_runless drops only worker_id, token binding remains; losing transaction rolls back whole :291-296/:347-353); supersession total-loss NEITHER (flip+append+record+completion one txn; fetch failures raise before DB write :224-248; all-SUPERSEDED display honest per C2); hostile sitemap BOUNDED (ET.fromstring on ≤5MB streamed body, expat amplification limits, pinned https://{registrable_domain}/sitemap.xml :404, check_url + robots + PII; type-safe lastmod compare; defusedxml optional hardening); SUPERSEDED leak NO (all consumers filtered; data-right export includes deliberately — correct; column+payload flipped together, no divergence writer); demo guard complete (both process entries; bootstrap_source_scans has no other importer; leaked scan = zero network via offline Fetcher).

NEW FINDINGS tickets: SEC-T32-02 (MEDIUM, follow-up) active_claims never maintained → purge protection vacuous + content-hash sweep dormant (F-1; fix: maintain counter or EXISTS(claims) liveness in purge); SEC-T32-03 (LOW, follow-up) recheck stale scan ignores SUPERSEDED (= reviewer #1); SEC-T32-04 (LOW) generation-key terminal-count race (F-4, self-healing); recheck 60s loop (F-5); stale routes_admin.py:15 docstring.

Guards not weakened: Fetcher untouched; store.py byte-untouched, token fence intact; T28 record/validators as contracted; paywall/tenancy ordering correct; migration reversible single head; A2/A3 facts-only (frozen_clock scoped to TestDemoRunIsByteIdentical.demo_session only; golden hash unchanged; negative control documented).

REQUIRED EXTRA TESTS (QA): (1) warm-cache staleness → origin-truth or clean no-change; (2) validators-match 304 completes touching nothing, no dead-letter; (3) purge skips live-claim-referenced page even with active_claims=0 (fails today; pins SEC-T32-02); (4) recheck_stale with SUPERSEDED row + fresh live claims takes no-op path (fails today; pins SEC-T32-03); (5) deterministic terminal-count race; (6) sitemap-index-only host bounded re-check.

## Reviewer A2 (agent_6e1bbf10-e65d-48fe-8b76-3ca42319c8d9) — VERDICT: PASS (on 1b363c707240a1f0fb92419b2f06eee4fed06399)

RC1 CLOSED: fetch carries use_cache=False + row validators (source_scanner.py:230-244); fetching.py:770-783 confirms warm cache skipped for ANY row; 304 = clean no-change (fetched_at direct, early return → SUCCEEDED in same txn worker.py:333-347; return precedes supersede query :272); skipping _record_page on 304 required and correct (record() overwrites validators unconditionally, models/source_page.py:124-137); if not res.ok raise retained :245-246. use_cache=False deviation RULED JUSTIFIED AND NECESSARY (packet-literal fix would pass all 3 committed tests while leaving validator-less rows warm-cache-readable — log as contract amendment). Regression check CLEAN (gates 17/96/102, mypy, ruff, single head; demo/offline unaffected; robots/PII/URL-policy not bypassed by use_cache=False). A1 followups (1)-(4) + SEC-T32-02/03/04 untouched/open. New nonblocking: N1 INFO wording nit (_check_page call normalization); N2 LOW 200-path record() can resurrect deleted row mid-flight (pre-existing A1, ticket alongside).

## Security A2 (agent_3bf76315-9aa4-49f8-8dba-9a249ca76652) — VERDICT: PASS (on 1b363c7)

RC1 CLOSED (same evidence chain; baseline verbatim confirmed at qa-a4 :223-225). NEW_SURFACE_REVIEW: validators same-origin only (redirect-stripped fetching.py:480-483; check_url/robots/PII unchanged); deleted-row+304 → StaleDataError fail path, no false success, no resurrection (adversarial 13/13, real ephemeral PG); URL/validators coherence (both enqueue sites pass page.id + page's own URL — routes_results.py:223-235, source_scanner.py:503); no Fetcher seam bypass; demo unchanged. CARRIED: SEC-T32-02/03/04 + reviewer followups open unchanged; 200-path resurrection residual pre-existing. GUARDS: store.py byte-identical (fenced writes compared directly), delta confined to reextract fetch+304 hunk, QA tests byte-identical to e8779da, single head d9c4e7a21b83. Required extra tests (QA queue): concurrent reextract same page; deterministic deleted-row retry leg; hostile oversized/CRLF validator values replayed; refresh amplification bound.

## Reviewer A3 (agent_871cd4c9-dfcd-4e84-a3c1-936e25a28e41) — VERDICT: PASS (on 9b362c823fa65bf3c95fe154419c8f6ba1b2dc51)

2-file type-only delta verified against tree reads (types.ts +3/−1 union member in backend enum order; format.ts +2 SUPERSEDED: neutral). Contract test asserts two-way set equality (test_frontend_contract.py:67) — fix is the correct remedy, assertion byte-intact. Exhaustive-consumer survey: claimStatusTone only; STATUS_LABEL/MEANING non-exhaustive by design. Gates on exact SHA from raw logs: contract 24, tsc/lint clean, vitest 182/182, build ok, api+pipeline 94. Security skip for A3 justified (zero behavior; A2 PASS covers unchanged production code). SHA proven via reflog (wip 6019460 off-branch). Nonblocking: SUPERSEDED gloss/label absent by design (planner follow-up); runtime chip rendering untested (no FE path produces one today).
