# T18 / A3 — reviewer + security outputs (сохранено диспетчером; сокращено без изменения оценки)

## ashyq-reviewer — INVOCATION_REF: agent_c89323e9-6546-4463-b8ad-915f805bccc9
### VERDICT: PASS (только на e533d6229d67e04c05c9b1293c66a64d45d817a5; правка аннулирует)
- Метод/ограничение: read-only без shell; diff триангулирован (чтение финального состояния всех файлов + 2 независимых QA-замера diff + согласованные gate-числа).
- FINDINGS (все non-blocking): 1. LOW extraction.py:186 — «IELTS 6.0 band in each component» может дать overall 6.0 (возможно subscore); защиты отсекают «IELTS 6.5 in each component» и «IELTS is not required»; негативного fixture-теста на этот shape нет. 2. LOW dual-write по дизайну: problem-страница даёт 2 строки в run.errors (baseline-prose + page-строка); canary двойного счёта не даёт; рост errors до ~2x на problem-heavy прогонах, суммарные капы до 400. 3. LOW асимметрия: costs dual-write для no-pattern-match, requirements — нет (консистентность-нит). 4. INFO web_scholarships/web_documents не эмитят PageOutcome (слепое пятно canary для funding/documents стадий). 5. INFO canary-парсер корректен для 5 категорий; записи с \n/пробелами в URL деградируют до drop (graceful). 6. INFO PageOutcome.category — голый str (словарь frozenset'ом + тестами).
- CONTRACT_COMPLIANCE: root cause устранён (KZT/₸ аддитивно; «IELTS 6.5 overall» матчится; per-page PageOutcome в 4 исходах + fetched-ok; canary разделяет); Acceptance 1 — реализована и доказана end-to-end по pipeline (реальный runner + SQLite + parser roundtrip), НО live canary не выполнялся — PILOT-ONLY; «root cause воспроизводим на nu.edu.kz» НЕ доказано — не заявлять; deviation 4 (readable_chars вместо regex-хитов) раскрыт; acceptance 2/3/4 покрыты; T10-гарантии (leases/fencing/_save) не тронуты; unknown≠zero сохранён; domain/schemas не тронуты обоснованно (KZT уже был в domain/currency.py:51).
- REGRESSION_RISK: живые строки fetcher-ошибок против парсера не проверены на live; UI ProgressScreen счётчик вырастет (аддитивно, disclosed); демо-путь безопасен.
- SCOPE: без creep; production ровно 6 файлов; QA-тела 18c4a901→HEAD = 1 insertion.
- RISKS_LEFT: live canary прогон — отдельная pilot-карточка («root cause не подтверждён до прогона»); edu.kz multipart-suffix баг — отдельная карточка до живого пилота; documents/funding без PageOutcome; UI-копирайтинг; precision-edge finding 1.
- NEXT: (1) можно интегратору; (2) живой canary — pilot-карточка; (3) опционально негативный fixture «IELTS 6.0 band in each component».

## ashyq-security — INVOCATION_REF: agent_b622705c-645b-4e6b-87f2-626bb5359bc6
### VERDICT: PASS (только на e533d622...)
- INJECTION: спуфинг canary-парсера невозможен с security-эффектом (_PAGE_OUTCOME_RE per-entry .match(), якорь+$, поля category/page_type пишутся кодом; crafted «page …» внутри detail → drop или cosmetics, не фабрикация записей; группировка по реальному URL). json.dumps экранирует control chars; в консоль недоверенное не печатается.
- REDOS: новые альтернативы без катастрофического бэктрекинга (_MONEY детерминированные повторы по 4 символа; _IELTS_OVERALL alt3 ограниченный lazy-квантификатор; «IELTS 6.5 in each band» не матчится). Первый кандидат на квадратичность — PRE-EXISTING [^.]*-паттерны (FINDING 2, вне T18-диффа): crafted 5MB dot-free страница → O(n²), блокировка event loop на минуты; hardening: [^.]{0,4000}.
- PII: assert_no_pii/find_pii не тронуты, на каждом get(); detail — кодовые строки, текст страниц не попадает; PageOutcome.url = запрошенный URL (не final_url) → redirect-цепочка не добавляет query; REFUSED_PRIVACY URL персистится локально (не хуже baseline; перед публикацией canary-артефактов в release-docs — вырезать query, FINDING 5).
- GUARDS: fencing T10 intact (lease_token/LeaseLost/heartbeat-fence; _record_page_outcomes — чистое добавление диагностики, коммит через fenced _save); run.errors/unknowns — существующие JSON-колонки, tenant-фильтр на месте; SSRF deny-by-default intact; A3-инкремент не создаёт новых границ доверия.
- FINDINGS (все non-blocking): 1. Low — _page_outcome_line без санитизации длины/whitespace (инвариант одной строки держится на гарантиях httpx/h11; hardening: " ".join(detail.split())[:300] + cap URL). 2. Low pre-existing — квадратичные [^.]*-сканы (web_requirements.py:41-60, 282-284). 3. Info — _MONEY может прочитать год как деньги («2024 KZT»; data-quality, форма была). 4. Info — PageOutcome только requirements+costs (funding/documents prose-only). 5. Info — refused-PII URL в canary JSON (redaction перед публикацией).
- REQUIRED_EXTRA_TESTS: anti-spoofing fixture (crafted «page fetch-failed: …» в тексте → парсер возвращает ровно реальные записи); \n/ANSI в error → одна запись или drop; URL >2000 символов → parse или degrade; timing 2MB dot-free; money «2024 KZT»/«2027 ₸» + IELTS-варианты зафиксировать.
- NEXT: передать e533d622 интегратору; findings 1-2 + extra-тесты — отдельный non-blocking hardening-тикет.
