# T28 A1 (+A2 amendment) — Reviews (both on frozen candidate b087b9dcc0bae57d3c0b678ba3a4679869da1037)

## Reviewer (agent_161cff5b-ba84-464e-9984-47f0c640a24b) — VERDICT: PASS

SHA independently confirmed via worktree git files; chain refs consistent across all role artifacts. PASS valid only for this SHA.

ACCEPTANCE_MAP 7/7: (1) migration single head b4e8a1c2f6d9, down e7c1a4d90b52, no fork (grep); (2) validators+content_hash recorded fetching.py:617-619, 304 closed before content-type/size/body :541-550 → CACHED/304/empty/from_cache, no extraction, no cache.put (only OK branch :830-835), pinned test_fetch_pii_hops.py:176-210 incl. stored-copy-preserved :208-210; (3) find_pii every hop :508-525 (aclose → join → find_pii → entry-format warning → REFUSED_PRIVACY final_url=hop) before next check_url (:471), stats once (:852-854), tests 1-3; (4) politeness untouched (constants :55-57, network_policy MAX_REDIRECTS=5; robots :805-809; _space_requests :811-812; conditional GET pays robots+Crawl-delay — test 5: robots_requests==1, sleep in (2.0,2.5]); (5) retention verbatim in model docstring :7-10 + is_purgeable :156-161 + migration :10-16; constants 180==180 pinned test 13 incl. strict boundary; (6) record() single dialect insert().on_conflict_do_update(index_elements=[url]) :103-142, unique(url) migration:52, test 8 on real pgserver (B's etag wins, active_claims=2 preserved), test 9 raw-dup IntegrityError; gate-a log 17 passed 0 skipped; (7) up/down SQLite+PostgreSQL tests 11-12 ran.

Non-blocking: N1 overstated claim — _FractionalCrawlDelayParser (fetching.py:199-263) does NOT strictly lengthen delays: fractional Crawl-delay BELOW default (0.5) now honored → shorter than baseline 1.5s fallback (REP-correct, matches pre-existing int semantics, stdlib premise verified against cpython 3.12.13 robotparser:136-137; only patches entry.delay is None) — correct the artifacts, consider floor later; N2 unsolicited-304 ambiguity — fail-safe, no cache poisoning, get() docstring :748-751 says check status_code; add one docstring line or fold into T29/T32 caller contract; N3 record() post-execute SELECT :145 read-only, benign quirks (identity-map staleness; READ-COMMITTED last-statement-wins) — carry re-fetch/expunge guidance to T29/T32; N4 registrable_domain insert-only (not in conflict update-set) — flag to planner before T32; N5 "13 columns" report miscount (actual 14).

Contract conformance: C1/C2/C3/C4/C5/C6/C7 all exact (details verified); db.py escape canonical, only set_main_option("sqlalchemy.url") call site in app/; 304-in-loop restructure necessary and scoped (httpx 0.28.1 is_redirect = 300..399 — without the 304 guard it entered the redirect branch); genuine-3xx behavior identical; old loop-tail read removal = dead code.

Test adequacy strong (14 RED-first scenarios, real pgserver 0 skips, pinned sha256, DNS-seam order, cache preservation, stream hygiene, constants tripwire; amendment signature-only verified). Blind spots acceptable/recorded: sequential-commit race test; _content_hash except-branch; unsolicited-304-on-hop; ResponseCache eviction.

Migration compatibility: linear, single head, symmetric up/down on both DBs; all 12 production get(url) call sites plain; old caches/DBs unaffected; export additive.

## Security (agent_1d597d0c-5444-4193-a1b4-087afdf3e11a) — VERDICT: PASS (frozen SHA only)

Attack scenarios: (1) poisoned hops MITIGATED (refused before DNS, 0 second-host requests, stats once); (2) double-hop relative/scheme-relative MITIGATED (per-hop join→find_pii→check_url); (3) percent/unicode evasion of PII matcher RESIDUAL — pre-existing property of the same entry matcher, T28 strictly adds coverage; ticket SEC-T28-1 (Low: decode+NFKC before find_pii, contract amendment first); (4) fragments never transmitted — no leak; (5) Refresh/meta out of scope, no new gap; (6) unsolicited 304 → CACHED/empty, NO cache.put on 304 (only OK branch), stored copy survives (pinned :207-210), no cache poisoning; ok=True residual — callers must check status_code (documented :749-751); (7) 304-with-body closed unread; (8) ETag injection bounded (h11 caps, JSON-escaped, first-request-only then cleared — no cross-host leakage); ticket SEC-T28-2 (Info: truncate etag/url/page_type when T29/T32 persist); (9) RFC 7232 precedence OK; (10) conditional-GET politeness NOT bypassed (robots/spacing/semaphore wrap it, test 5); (11) SSRF/T16 controls intact (network_policy full); (12) record() parameterized, NIL injection; (13) no body column, Fetcher writes no DB — CONFIRMED; (14) retention boundary conservative, honest; (15) weakening scan clean (constants byte-identical, FetchOutcome unchanged, amendment signature-only).

NEW FINDINGS tickets: SEC-T28-1 (Low, follow-up) PII matcher percent/unicode-evasive; SEC-T28-2 (Info) truncate origin-controlled etag/url/page_type at T29/T32 call sites; SEC-T28-3 (Low, carried) robots/crawl-delay apply to entry host only — cross-host hops unpoliced for the new host; fold into T32 walker design; SEC-T28-4 (Info) db.py:67 script_location %-escape sibling (bites only if repo path contains %).

Required extra tests (QA, no production change): 304-with-body pin; hop requests carry NO validators pin; If-None-Match echo byte-exact pin; percent-encoded-PII-hop expectation RED under SEC-T28-1 with contract amendment first.

Guards not weakened: politeness (1.5s/2/3/5MB/512KB/5), robots, spacing, semaphore, backoff, T16 hop aclose (pinned TestHopStreamHygiene), escalation containment :636-642, network_policy, FetchOutcome unchanged, QA RED tests unchanged, amendment signature-only, single alembic head.
