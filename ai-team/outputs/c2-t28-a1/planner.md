# T28 A1 — Planner contract (frozen)

- Invocation ref: agent_a194d558-739d-4be6-8321-35f940c4cdf4
- Baseline: 28d729ca76cdf8d52344517a8316e2e805099d11 (ai/c2/integration head)
- STATUS: PLANNED
- CORRECTION by dispatcher (post-contract): planner read fetching.py in the main checkout which was at 4d2125c (pre-T16). At the true baseline 28d729c the redirect branch ALREADY closes hop streams (fetching.py:363,374 — T16's R3 fix) — planner's "adjacent defect" C6 clause is therefore already satisfied; treat stream-closure as justified-GREEN, do not re-implement. The find_pii-on-hops gap IS real (find_pii only at :564 entry; hop loop at :339-380 does not call it). Line numbers in planner facts are pre-T16 and shifted; QA/developer must work against the actual worktree files.

## Facts (per planner, with the correction above)

- find_pii at entry only (fetching.py:564 post-T16 numbering); hop loop per-hop check_url (:350 pre-T16) but no find_pii → hop ?token=... is fetched today.
- Entry-level refusal semantics to mirror: REFUSED_PRIVACY outcome, stats bucket, log.warning format (fetching.py:548-556 pre-T16), find_pii/assert_no_pii :267-294, patterns :252-264.
- Politeness to keep byte-identical: robots per get(), _space_requests Crawl-delay/1.5s, per-host semaphore 2 (MAX_PER_HOST_CONCURRENCY), MAX_ATTEMPTS=3 backoff 2s/4s, MAX_REDIRECTS=5.
- FetchResult (fetching.py:93-115): no etag/last_modified/content_hash; ok True for OK+CACHED.
- ResponseCache: disk TTL 86400, meta url/final_url/status_code/content_type/fetched_at/stored_at/encoding; unbounded (audit gap).
- _maybe_render: browser output has no HTTP validators → validators must reset on escalation.
- All 12 production call sites use plain await fetcher.get(url) → additive keyword-only params backward compatible.
- Alembic: 14 revisions, single head e7c1a4d90b52 (subscriptions). Chain recorded in planner output.
- Models: TimestampedBase (id String(32), created_at, updated_at); enums as plain strings; named unique Index pattern (research.py:31-36, 109-111); no adapter imports in models; SourcePage must be added to models/__init__.py; page_type as String(40) (no PageType import).
- registrable_domain derivation lives in live_discovery.py:251-284; institution_key does not exist yet (caller-supplied; NULL = unmapped).
- conftest.py already provides postgres_url/pg_engine/pg_session (pgserver) — no conftest edits needed; second session via sessionmaker(bind=pg_engine).
- Precedents: IntegrityError dup test test_jobs.py:363-395; head check :397-404; migration up/down + model agreement test_social_models.py:47-65, 91-103; MockTransport + monkeypatched check_url test_ssrf.py:210-284 (copy helpers, do NOT edit test_ssrf.py).

## Frozen contract

- C1 NEW backend/app/models/source_page.py: SourcePage(TimestampedBase): url Text NOT NULL unique; registrable_domain String(255) NOT NULL (caller-derived); institution_key String(120) NULL; etag Text NULL; last_modified_header Text NULL; content_hash String(64) NULL (sha256 hex); lastmod_seen DateTime(tz) NULL; fetched_at DateTime(tz) NULL; http_status Integer NULL; page_type String(40) NOT NULL default "unknown"; active_claims Integer NOT NULL default 0 (never touched by Fetcher/record). Indexes: uq_source_pages_url unique(url), ix_source_pages_domain, ix_source_pages_institution, ix_source_pages_retention(active_claims, fetched_at). Unique on url ALONE (mutable one-row-per-page per design §4.2; version-history would break active_claims/retention).
- C2 Writer seam: SourcePage.record(session, *, url, registrable_domain, etag=None, last_modified_header=None, content_hash=None, http_status=None, page_type="unknown", institution_key=None, lastmod_seen=None, fetched_at=None) -> SourcePage. Atomic upsert insert().on_conflict_do_update (pg+sqlite); NO select-then-insert. Conflict update-set: etag/last_modified_header/content_hash/http_status/page_type/fetched_at = passed values (None overwrites); institution_key/lastmod_seen coalesce (existing wins over None); active_claims NEVER touched. Flush, no commit; caller owns transaction. No production caller in T28 (T29/T32 wire it). Callers SHOULD pass canonical_url.
- C3 FetchResult += etag/last_modified/content_hash (additive, defaults ""). 304 → outcome=CACHED, status_code=304, content=b"", text="", from_cache=True, error="not modified (304)". NO new enum member. Consumers MUST check status_code==304 and not read text. ResponseCache.put/get store/restore new meta keys; old entries restore defaults.
- C4 content_hash = sha256( NFKC(" ".join(text.split())).encode("utf-8") ).hexdigest() over html_to_text(result.text) (lazy import already used :453); fallback to result.text if html_to_text fails or PDF; documented self-consistency (drift → one redundant re-extraction, never corruption). Escalation: rendered result → content_hash recomputed from rendered text, etag/last_modified reset to "". Fixtures never hashed/written.
- C5 Conditional GET: get(self, url, *, use_cache=True, etag=None, if_modified_since=None). Validators passed by CALLER (from source_pages); Fetcher never touches DB. Validators present → disk-cache read skipped; robots/_space_requests/semaphore/pinning/per-hop check_url all still apply (304 costs a full polite request). Headers on FIRST request only (hop requests unconditional — no cross-host validator leakage). If-None-Match precedence per RFC 7232. 304: terminal success, no retry/backoff/cache.put/escalation, stats[CACHED]+=_; _read_response must test 304 BEFORE content-type/size/body branch. 200: populate etag/last_modified/content_hash. T28 ships primitive only — no source_scan/reextract (T32).
- C6 find_pii on every hop: after current=join(location), before next check_url: leak=find_pii(current); if leak → close redirect response, stats[REFUSED_PRIVACY]+=_, same log.warning format, return FetchResult(url=url, outcome=REFUSED_PRIVACY, error="Refused: the URL contains what looks like a {leak}. It was not fetched.", final_url=current). No raise (crawl-discovered links skipped, not fatal). Robots-per-hop stays out of scope (residual, unchanged). Stream closure on redirect paths: ALREADY DONE by T16 — justified-GREEN.
- C7 Retention policy (text verbatim in migration + model docstrings): metadata-only rows (never bodies); purgeable when active_claims=0 AND fetched_at < now()-RETENTION_DAYS, or fetched_at IS NULL AND created_at < now()-RETENTION_DAYS; active_claims>0 never purged; RETENTION_DAYS=180 (owner may override 90); purge execution is T32's job. Constants RETENTION_DAYS=180 in model, SOURCE_PAGE_RETENTION_DAYS=180 in migration, QA agreement test; pure helper is_purgeable(fetched_at, created_at, active_claims, now) in model.
- Migration: exactly one new head, down_revision="e7c1a4d90b52"; upgrade create table+4 indexes, downgrade drop; heads count 1 before and after; T26/T32 must not claim e7c1a4d90b52 while T28 in flight (integrator rebases if needed).
- allowed_paths: fetching.py, models/** (source_page.py new + __init__.py export), migrations/versions/<new>_source_pages.py (one file), tests/test_fetch*.py (new), tests/test_source_pages.py (new). Forbidden/unnecessary: conftest.py, test_ssrf.py, test_jobs.py, extraction.py, web_requirements.py, claim_verifier.py, runner.py, browser.py, live_discovery.py (import-only), page_classifier.py (import-only), alembic.ini/env.py.
- Cross-task note: T28 imports html_to_text from app.adapters.extraction (pre-existing lazy import) — T27 owns that file; if T27 moves the symbol, T27's change wins and T28 adjusts one import line.

## QA scenarios (C10, named)

test_fetch_pii_hops.py: (1) PII param on redirect hop → REFUSED_PRIVACY, request-count to second host == 0, stats bucket, content b"" [RED]; (2) 9+ digit query on hop → refused [RED]; (3) hop check runs before DNS: check_url not called with poisoned URL [guards seam]; (4) 200 etag v1 then 304 on If-None-Match → CACHED/304/empty text/from_cache [RED: no etag param]; (5) conditional request still pays politeness (robots+spacing counters applied) [GREEN-after guard]; (6) 200 records validators + pin exact sha256 hex literal; escalated result has etag "" + recomputed hash; (7) politeness constants tripwire: MAX_PER_HOST_CONCURRENCY==2, DEFAULT_DELAY_SECONDS==1.5, MAX_REDIRECTS==5, MAX_ATTEMPTS==3.
test_source_pages.py: (8) two concurrent sessions record same url once (pg_engine + 2 sessions) [RED: no model]; (9) raw dup insert → IntegrityError, count==1; (10) upsert semantics: institution_key/lastmod_seen coalesce, etag None overwrites, active_claims never changes; (11) upgrade/downgrade SQLite (migrate_to_head; downgrade to e7c1a4d90b52; table gone; re-upgrade); (12) same on PostgreSQL URL; (13) retention constant agreement + is_purgeable truth table (boundary at exactly RETENTION_DAYS not purgeable); (14) exactly one alembic head == app.db.head_revision().

Product decision: RETENTION_DAYS=180 recommended (owner may override 90; constant+docstring only).
