# Baseline verification — 2026-08-29

Re-run from scratch on `main` at `ea7948c` before any change on
`claude/production-completion`. Every row is a command that was actually run.

| Check | Command | Result |
|---|---|---|
| Worktree | `git status --porcelain` | clean, 0 entries |
| Branch / HEAD | `git log --oneline -1` | `ea7948c` on `main`, matches expected baseline |
| In-progress ops | `.git/{MERGE,CHERRY_PICK,REVERT}_HEAD`, `rebase-*` | none |
| ruff | `.venv/bin/ruff check app tests scripts` | All checks passed |
| mypy | `.venv/bin/mypy app tests scripts/canary_discovery.py` | Success, 91 source files |
| Backend SQLite | `UNIMATCH_DATABASE_URL=sqlite:///... pytest` | **553 passed** |
| Backend coverage | `pytest --cov=app --cov-fail-under=80` | **90.02%** |
| Backend PostgreSQL | `.venv/bin/python scripts/pg.py .venv/bin/pytest` | **553 passed** |
| Frontend typecheck | `npm run typecheck` | clean |
| Frontend lint | `npm run lint` | clean |
| Frontend unit | `npm test -- --run` | **47 passed** (4 files) |
| Frontend build | `npm run build` | 249.92 kB raw / **74.04 kB gzip** JS, 5.28 kB gzip CSS |
| E2E | `npm run e2e` | **50 passed** (41.6s) |
| pip-audit | `pip-audit -r backend/requirements.txt` | No known vulnerabilities |
| npm audit | `npm audit --audit-level=high` | 0 vulnerabilities |

The task brief expected 547 backend tests; the repository actually has 553.
The brief's other figures match.

## Environment blockers found at baseline

| Blocker | Evidence | Consequence |
|---|---|---|
| **No container runtime** | `docker`, `podman`, `colima`, `nerdctl`, `lima` all absent from PATH; no `/Applications/Docker.app`, no OrbStack | The production-shaped runtime gate (compose up, health, crash, backup/restore) **cannot be executed here**. Installing one needs an admin password, which is a user checkpoint. Recorded as **BLOCKED**, never as PASS. |

## Live services left alone

| Port / process | What it is | Action |
|---|---|---|
| 8199 (PID 33546) | the public demo behind the Cloudflare tunnel | untouched |
| PID 33441 | `cloudflared tunnel` | untouched |

All work below uses temporary databases and non-conflicting ports.
