# Pinned. `pip-audit` runs in CI and a new advisory fails the build, so these
# floors exist for a reason - see docs/adr/ and SECURITY.md before lowering one.
fastapi>=0.121.0
uvicorn[standard]==0.34.0
starlette>=1.3.1            # PYSEC-2026-161/248/249/1941/1942/2280/2281
pydantic==2.13.4
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.3.4
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==6.1.0
pypdf>=6.15.0              # 20 advisories incl. malformed-PDF DoS in our parse path
openpyxl==3.1.5
python-multipart>=0.0.31   # PYSEC-2026-1852/3036-3040
tenacity==9.0.0
playwright==1.60.0

# Public Suffix List, bundled offline. Replaces a hand-built suffix table that
# silently treated institutions under unlisted suffixes as foreign domains.
# No runtime network access; the version pins a dated PSL snapshot.
publicsuffixlist==1.0.2.20260821
