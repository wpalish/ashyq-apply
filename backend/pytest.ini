[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    slow: drives real processes or the network; seconds, not milliseconds
filterwarnings =
    ignore::DeprecationWarning
