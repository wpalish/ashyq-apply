# Token reference

> Generated by `node scripts/generate-token-reference.js` from `frontend/src/styles/tokens.css`. Layer 1 variables define upstream primitives. Layer 2 variables are the only variables component CSS may reference.

## Layer 1 — upstream design-system primitives

| Variable | Default value | Dark override | When to use |
|---|---|---|---|
| `--ds-color-brand-ink` | `#111827` | — | Exact brandbook v1.1 primitive. Keep the value exact; components consume its semantic Layer 2 alias. |
| `--ds-color-brand-paper` | `#f7f3ea` | — | Exact brandbook v1.1 primitive. Keep the value exact; components consume its semantic Layer 2 alias. |
| `--ds-color-brand-academic-blue` | `#526da6` | — | Exact brandbook v1.1 primitive. Keep the value exact; components consume its semantic Layer 2 alias. |
| `--ds-color-brand-library-burgundy` | `#8e3f4c` | — | Exact brandbook v1.1 primitive. Keep the value exact; components consume its semantic Layer 2 alias. |
| `--ds-color-brand-scholar-teal` | `#437a78` | — | Exact brandbook v1.1 primitive. Keep the value exact; components consume its semantic Layer 2 alias. |
| `--ds-color-brand-parchment-gold` | `#c5a66b` | — | Exact brandbook v1.1 primitive. Keep the value exact; components consume its semantic Layer 2 alias. |
| `--ds-color-brand-deadline-coral` | `#e47a6a` | — | Exact brandbook v1.1 primitive. Keep the value exact; components consume its semantic Layer 2 alias. |
| `--ds-color-paper` | `var(--ds-color-brand-paper)` | `#0b1220` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-surface` | `#fffcf6` | `var(--ds-color-brand-ink)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-surface-sunken` | `#efe9de` | `#080e19` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-surface-raised` | `#ffffff` | `#1b2535` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-surface-inverse` | `var(--ds-color-brand-ink)` | `var(--ds-color-brand-paper)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-ink` | `var(--ds-color-brand-ink)` | `var(--ds-color-brand-paper)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-ink-muted` | `#5d6470` | `#c4cbd6` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-ink-subtle` | `#636b77` | `#aeb7c5` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-ink-inverse` | `var(--ds-color-brand-paper)` | `var(--ds-color-brand-ink)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-rule` | `#ded6c8` | `#2e3a4d` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-rule-strong` | `#c7bdae` | `#46566d` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-accent` | `var(--ds-color-brand-academic-blue)` | `#9cb2e1` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-accent-hover` | `#3f5885` | `#b9c8e8` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-accent-soft` | `#e8ecf5` | `#1e2c46` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-accent-border` | `#aab8d9` | `var(--ds-color-brand-academic-blue)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-success` | `#376b69` | `#8fc7c4` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-success-soft` | `#e5efec` | `#173331` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-success-border` | `var(--ds-color-brand-scholar-teal)` | `var(--ds-color-brand-scholar-teal)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-info` | `#4b6599` | `#9cb2e1` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-info-soft` | `#e8ecf5` | `#1e2c46` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-info-border` | `#aab8d9` | `var(--ds-color-brand-academic-blue)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-warning` | `#725a2a` | `#e1c88f` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-warning-soft` | `#f4ebd7` | `#372f20` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-warning-border` | `var(--ds-color-brand-parchment-gold)` | `var(--ds-color-brand-parchment-gold)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-danger` | `var(--ds-color-brand-library-burgundy)` | `#f0b5bd` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-danger-soft` | `#f4e6e8` | `#3d242b` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-danger-border` | `#c8919a` | `var(--ds-color-brand-library-burgundy)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-deadline` | `#8b4338` | `#f2a79d` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-deadline-soft` | `#f8e4df` | `#402722` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-deadline-border` | `var(--ds-color-brand-deadline-coral)` | `var(--ds-color-brand-deadline-coral)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-neutral` | `#5f6673` | `#c5cbd5` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-neutral-soft` | `#ebe8e2` | `#273142` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-neutral-border` | `#c9c4ba` | `#475569` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-demo` | `var(--ds-color-brand-library-burgundy)` | `#d7a7b0` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-demo-soft` | `#f4e6e8` | `#3b2530` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-demo-border` | `#c8919a` | `var(--ds-color-brand-library-burgundy)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-overlay` | `rgb(0 0 0 / 60%)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-white` | `oklch(99% 0 0)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-on-accent` | `#ffffff` | `var(--ds-color-brand-ink)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-transparent` | `transparent` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-color-inherit` | `inherit` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-family-brand` | `'Manrope', 'Inter', system-ui, sans-serif` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-family-display` | `'Fraunces', 'Iowan Old Style', Georgia, serif` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-family-ui` | `'Inter', system-ui, -apple-system, 'Segoe UI', sans-serif` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-family-mono` | `'IBM Plex Mono', 'JetBrains Mono', ui-monospace, 'SF Mono', Menlo, monospace` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-2xs` | `0.75rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-micro` | `0.75rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-xs` | `0.875rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-sm` | `0.875rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-base` | `1rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-md` | `1.125rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-lg` | `clamp(1.125rem, 1rem + 0.5vw, 1.375rem)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-xl` | `clamp(1.5rem, 1.25rem + 1vw, 1.75rem)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-display` | `clamp(2.25rem, 1.6rem + 2.5vw, 3rem)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-brand` | `1.75rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-stat` | `2.25rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-size-code` | `0.875em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-weight-medium` | `500` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-weight-semibold` | `600` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-font-weight-bold` | `700` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-line-height-flat` | `1` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-line-height-tight` | `1.15` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-line-height-caption` | `1.3` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-line-height-snug` | `1.35` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-line-height-compact` | `1.55` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-line-height-normal` | `1.6` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-letter-spacing-tight` | `-0.015em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-letter-spacing-display` | `-0.03em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-letter-spacing-avatar` | `0.02em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-letter-spacing-label` | `0.05em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-letter-spacing-medium` | `0.07em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-letter-spacing-wide` | `0.08em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-letter-spacing-wider` | `0.09em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-letter-spacing-widest` | `0.12em` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-0` | `0` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-auto` | `auto` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-negative-hairline` | `-1px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-hairline` | `1px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-0-5` | `2px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-0-75` | `3px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-1` | `0.25rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-1-25` | `5px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-1-5` | `0.375rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-2` | `0.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-2-5` | `0.625rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-3` | `0.75rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-4` | `1rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-5` | `1.25rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-6` | `1.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-7` | `2rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-8` | `3rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-9` | `clamp(3rem, 2rem + 4vw, 5.5rem)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-control-block` | `0.42rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-control-inline` | `0.6rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-control-compact` | `0.35rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-dot-offset` | `0.42rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-checkbox-offset` | `0.2rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-panel-inset` | `0.18rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-button-block` | `0.44rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-button-inline` | `0.9rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-button-compact-inline` | `0.6rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-table-block` | `0.45rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-table-inline` | `0.3rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-table-wide-inline` | `0.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-stage-dot` | `0.7rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-space-check` | `0.1rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-border-width-hairline` | `1px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-border-width-medium` | `2px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-border-width-strong` | `3px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-border-width-accent` | `4px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-sm` | `4px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-xs` | `4px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-none` | `0` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-md` | `8px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-lg` | `12px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-xl` | `16px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-page` | `24px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-full` | `999px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-radius-round` | `50%` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-shadow-none` | `none` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-shadow-sm` | `0 1px 2px oklch(23% 0.018 265 / 0.06)` | `0 1px 2px oklch(0% 0 0 / 0.30)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-shadow-md` | `0 2px 6px oklch(23% 0.018 265 / 0.08), 0 1px 2px oklch(23% 0.018 265 / 0.05)` | `0 2px 8px oklch(0% 0 0 / 0.38), 0 1px 2px oklch(0% 0 0 / 0.25)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-shadow-lg` | `0 12px 32px oklch(23% 0.018 265 / 0.13), 0 2px 8px oklch(23% 0.018 265 / 0.07)` | `0 14px 38px oklch(0% 0 0 / 0.48), 0 2px 8px oklch(0% 0 0 / 0.30)` | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-shadow-focus` | `0 0 0 3px var(--ds-color-accent-soft)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-shadow-nav-active` | `inset 3px 0 0 var(--ds-color-accent)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-shadow-nav-active-mobile` | `inset 0 -2px 0 var(--ds-color-accent)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-z-base` | `1` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-z-sticky-cell` | `2` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-z-sticky-edge` | `3` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-z-header` | `20` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-z-modal` | `50` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-duration-reduced` | `0.01ms` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-duration-fast` | `120ms` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-duration-normal` | `180ms` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-duration-pulse` | `1.4s` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-duration-spin` | `0.8s` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-ease-standard` | `ease` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-ease-linear` | `linear` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-ease-out-expo` | `cubic-bezier(0.16, 1, 0.3, 1)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-sidebar` | `15.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-measure` | `68ch` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-auth-card` | `30rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-content` | `52rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-reading-column` | `46rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-profile-bio` | `62ch` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-table-default` | `64.25rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-table-ranked` | `69.75rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-column-xs` | `4.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-column-sm` | `6rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-column-md` | `6.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-column-lg` | `6.75rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-column-xl` | `7.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-badge-min` | `1.1rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-stage-track` | `1.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-control-min` | `9.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-grid-sm` | `9rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-grid-md` | `11rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-grid-lg` | `13rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-person-card` | `19rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-university-min` | `10rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-university-max` | `18rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-bubble-max` | `34rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-icon` | `1rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-touch-target` | `2.75rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-content-wide` | `90rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-swatch-lg` | `7.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-open-page` | `4rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-status-icon` | `1.5rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-avatar` | `2rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-avatar-lg` | `3rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-fund-bar` | `1.4rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-stage-dot` | `0.7rem` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-meter` | `6px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-swatch` | `10px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-size-visually-hidden` | `1px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-blur-header` | `8px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-translate-pressed` | `1px` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-background-none` | `none` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-background-app-texture` | `none` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-background-meter` | `var(--ds-color-accent)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |
| `--ds-background-unknown-pattern` | `var(--ds-color-neutral-soft)` | — | Upstream primitive. Define or theme at the design-system boundary; never reference directly from component CSS. |

## Layer 2 — ASHYQ Apply project aliases

| Variable | Value | When to use |
|---|---|---|
| `--color-brand-ink` | `var(--ds-color-brand-ink, #111827)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-brand-paper` | `var(--ds-color-brand-paper, #f7f3ea)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-brand-academic-blue` | `var(--ds-color-brand-academic-blue, #526da6)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-brand-library-burgundy` | `var(--ds-color-brand-library-burgundy, #8e3f4c)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-brand-scholar-teal` | `var(--ds-color-brand-scholar-teal, #437a78)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-brand-parchment-gold` | `var(--ds-color-brand-parchment-gold, #c5a66b)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-brand-deadline-coral` | `var(--ds-color-brand-deadline-coral, #e47a6a)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-background` | `var(--ds-color-paper, #f7f3ea)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-surface` | `var(--ds-color-surface, #fffcf6)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-surface-sunken` | `var(--ds-color-surface-sunken, #efe9de)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-surface-raised` | `var(--ds-color-surface-raised, #ffffff)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-surface-inverse` | `var(--ds-color-surface-inverse, #111827)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-text` | `var(--ds-color-ink, #111827)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-text-muted` | `var(--ds-color-ink-muted, #5d6470)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-text-subtle` | `var(--ds-color-ink-subtle, #636b77)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-text-inverse` | `var(--ds-color-ink-inverse, #f7f3ea)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-border` | `var(--ds-color-rule, #ded6c8)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-border-strong` | `var(--ds-color-rule-strong, #c7bdae)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-link` | `var(--ds-color-accent, #526da6)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-link-hover` | `var(--ds-color-accent-hover, #3f5885)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-interactive` | `var(--ds-color-accent, #526da6)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-interactive-hover` | `var(--ds-color-accent-hover, #3f5885)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-interactive-soft` | `var(--ds-color-accent-soft, #e8ecf5)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-interactive-border` | `var(--ds-color-accent-border, #aab8d9)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-focus-ring` | `var(--ds-color-accent, #526da6)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-success` | `var(--ds-color-success, #376b69)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-success-soft` | `var(--ds-color-success-soft, #e5efec)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-success-border` | `var(--ds-color-success-border, #437a78)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-info` | `var(--ds-color-info, #4b6599)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-info-soft` | `var(--ds-color-info-soft, #e8ecf5)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-info-border` | `var(--ds-color-info-border, #aab8d9)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-warning` | `var(--ds-color-warning, #725a2a)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-warning-soft` | `var(--ds-color-warning-soft, #f4ebd7)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-warning-border` | `var(--ds-color-warning-border, #c5a66b)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-danger` | `var(--ds-color-danger, #8e3f4c)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-danger-soft` | `var(--ds-color-danger-soft, #f4e6e8)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-danger-border` | `var(--ds-color-danger-border, #c8919a)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-deadline` | `var(--ds-color-deadline, #8b4338)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-deadline-soft` | `var(--ds-color-deadline-soft, #f8e4df)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-deadline-border` | `var(--ds-color-deadline-border, #e47a6a)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-neutral` | `var(--ds-color-neutral, #5f6673)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-neutral-soft` | `var(--ds-color-neutral-soft, #ebe8e2)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-neutral-border` | `var(--ds-color-neutral-border, #c9c4ba)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-demo` | `var(--ds-color-demo, #8e3f4c)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-demo-soft` | `var(--ds-color-demo-soft, #f4e6e8)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-demo-border` | `var(--ds-color-demo-border, #c8919a)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-overlay` | `var(--ds-color-overlay, rgb(0 0 0 / 60%))` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-on-interactive` | `var(--ds-color-on-accent, #ffffff)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-transparent` | `var(--ds-color-transparent, transparent)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--color-inherit` | `var(--ds-color-inherit, inherit)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-family-brand` | `var(--ds-font-family-brand, 'Manrope', system-ui, sans-serif)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-family-display` | `var(--ds-font-family-display, Georgia, serif)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-family-ui` | `var(--ds-font-family-ui, system-ui, sans-serif)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-family-mono` | `var(--ds-font-family-mono, ui-monospace, monospace)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-2xs` | `var(--ds-font-size-2xs, 0.75rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-micro` | `var(--ds-font-size-micro, 0.75rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-xs` | `var(--ds-font-size-xs, 0.875rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-sm` | `var(--ds-font-size-sm, 0.875rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-base` | `var(--ds-font-size-base, 1rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-md` | `var(--ds-font-size-md, 1.125rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-lg` | `var(--ds-font-size-lg, 1.375rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-xl` | `var(--ds-font-size-xl, 1.75rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-display` | `var(--ds-font-size-display, 3rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-brand` | `var(--ds-font-size-brand, 1.75rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-stat` | `var(--ds-font-size-stat, 2.25rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-size-code` | `var(--ds-font-size-code, 0.875em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-weight-medium` | `var(--ds-font-weight-medium, 500)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-weight-semibold` | `var(--ds-font-weight-semibold, 600)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--font-weight-bold` | `var(--ds-font-weight-bold, 700)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--line-height-flat` | `var(--ds-line-height-flat, 1)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--line-height-tight` | `var(--ds-line-height-tight, 1.15)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--line-height-caption` | `var(--ds-line-height-caption, 1.3)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--line-height-snug` | `var(--ds-line-height-snug, 1.35)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--line-height-compact` | `var(--ds-line-height-compact, 1.55)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--line-height-normal` | `var(--ds-line-height-normal, 1.6)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--letter-spacing-tight` | `var(--ds-letter-spacing-tight, -0.015em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--letter-spacing-display` | `var(--ds-letter-spacing-display, -0.03em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--letter-spacing-avatar` | `var(--ds-letter-spacing-avatar, 0.02em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--letter-spacing-label` | `var(--ds-letter-spacing-label, 0.05em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--letter-spacing-medium` | `var(--ds-letter-spacing-medium, 0.07em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--letter-spacing-wide` | `var(--ds-letter-spacing-wide, 0.08em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--letter-spacing-wider` | `var(--ds-letter-spacing-wider, 0.09em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--letter-spacing-widest` | `var(--ds-letter-spacing-widest, 0.12em)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-0` | `var(--ds-space-0, 0)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-auto` | `var(--ds-space-auto, auto)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-negative-hairline` | `var(--ds-space-negative-hairline, -1px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-hairline` | `var(--ds-space-hairline, 1px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-0-5` | `var(--ds-space-0-5, 2px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-0-75` | `var(--ds-space-0-75, 3px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-1` | `var(--ds-space-1, 0.25rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-1-25` | `var(--ds-space-1-25, 5px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-1-5` | `var(--ds-space-1-5, 0.375rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-2` | `var(--ds-space-2, 0.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-2-5` | `var(--ds-space-2-5, 0.625rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-3` | `var(--ds-space-3, 0.75rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-4` | `var(--ds-space-4, 1rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-5` | `var(--ds-space-5, 1.25rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-6` | `var(--ds-space-6, 1.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-7` | `var(--ds-space-7, 2rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-8` | `var(--ds-space-8, 3rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-9` | `var(--ds-space-9, 5.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-control-block` | `var(--ds-space-control-block, 0.42rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-control-inline` | `var(--ds-space-control-inline, 0.6rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-control-compact` | `var(--ds-space-control-compact, 0.35rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-dot-offset` | `var(--ds-space-dot-offset, 0.42rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-checkbox-offset` | `var(--ds-space-checkbox-offset, 0.2rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-panel-inset` | `var(--ds-space-panel-inset, 0.18rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-button-block` | `var(--ds-space-button-block, 0.44rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-button-inline` | `var(--ds-space-button-inline, 0.9rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-button-compact-inline` | `var(--ds-space-button-compact-inline, 0.6rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-table-block` | `var(--ds-space-table-block, 0.45rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-table-inline` | `var(--ds-space-table-inline, 0.3rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-table-wide-inline` | `var(--ds-space-table-wide-inline, 0.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-stage-dot` | `var(--ds-space-stage-dot, 0.7rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--space-check` | `var(--ds-space-check, 0.1rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--border-width-hairline` | `var(--ds-border-width-hairline, 1px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--border-width-medium` | `var(--ds-border-width-medium, 2px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--border-width-strong` | `var(--ds-border-width-strong, 3px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--border-width-accent` | `var(--ds-border-width-accent, 4px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-sm` | `var(--ds-radius-sm, 4px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-xs` | `var(--ds-radius-xs, 4px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-none` | `var(--ds-radius-none, 0)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-md` | `var(--ds-radius-md, 8px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-lg` | `var(--ds-radius-lg, 12px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-xl` | `var(--ds-radius-xl, 16px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-page` | `var(--ds-radius-page, 24px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-full` | `var(--ds-radius-full, 999px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--radius-round` | `var(--ds-radius-round, 50%)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--shadow-none` | `var(--ds-shadow-none, none)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--shadow-sm` | `var(--ds-shadow-sm, none)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--shadow-md` | `var(--ds-shadow-md, none)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--shadow-lg` | `var(--ds-shadow-lg, none)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--shadow-focus-ring` | `var(--ds-shadow-focus, 0 0 0 3px var(--color-interactive-soft))` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--shadow-nav-active` | `var(--ds-shadow-nav-active, inset 3px 0 0 var(--color-interactive))` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--shadow-nav-active-mobile` | `var(--ds-shadow-nav-active-mobile, inset 0 -2px 0 var(--color-interactive))` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--z-base` | `var(--ds-z-base, 1)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--z-sticky-cell` | `var(--ds-z-sticky-cell, 2)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--z-sticky-edge` | `var(--ds-z-sticky-edge, 3)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--z-header` | `var(--ds-z-header, 20)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--z-modal` | `var(--ds-z-modal, 50)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--motion-duration-reduced` | `var(--ds-duration-reduced, 0.01ms)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--motion-duration-fast` | `var(--ds-duration-fast, 120ms)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--motion-duration-normal` | `var(--ds-duration-normal, 180ms)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--motion-duration-pulse` | `var(--ds-duration-pulse, 1.4s)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--motion-duration-spin` | `var(--ds-duration-spin, 0.8s)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--motion-ease-standard` | `var(--ds-ease-standard, ease)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--motion-ease-linear` | `var(--ds-ease-linear, linear)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--motion-ease-out-expo` | `var(--ds-ease-out-expo, cubic-bezier(0.16, 1, 0.3, 1))` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-sidebar` | `var(--ds-size-sidebar, 15.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-measure` | `var(--ds-size-measure, 68ch)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-auth-card` | `var(--ds-size-auth-card, 30rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-content` | `var(--ds-size-content, 52rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-reading-column` | `var(--ds-size-reading-column, 46rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-profile-bio` | `var(--ds-size-profile-bio, 62ch)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-table-default` | `var(--ds-size-table-default, 64.25rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-table-ranked` | `var(--ds-size-table-ranked, 69.75rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-column-xs` | `var(--ds-size-column-xs, 4.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-column-sm` | `var(--ds-size-column-sm, 6rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-column-md` | `var(--ds-size-column-md, 6.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-column-lg` | `var(--ds-size-column-lg, 6.75rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-column-xl` | `var(--ds-size-column-xl, 7.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-badge-min` | `var(--ds-size-badge-min, 1.1rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-stage-track` | `var(--ds-size-stage-track, 1.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-control-min` | `var(--ds-size-control-min, 9.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-grid-sm` | `var(--ds-size-grid-sm, 9rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-grid-md` | `var(--ds-size-grid-md, 11rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-grid-lg` | `var(--ds-size-grid-lg, 13rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-person-card` | `var(--ds-size-person-card, 19rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-university-min` | `var(--ds-size-university-min, 10rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-university-max` | `var(--ds-size-university-max, 18rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-bubble-max` | `var(--ds-size-bubble-max, 34rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-icon` | `var(--ds-size-icon, 1rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-touch-target` | `var(--ds-size-touch-target, 2.75rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-content-wide` | `var(--ds-size-content-wide, 90rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-swatch-lg` | `var(--ds-size-swatch-lg, 7.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-open-page` | `var(--ds-size-open-page, 4rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-status-icon` | `var(--ds-size-status-icon, 1.5rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-avatar` | `var(--ds-size-avatar, 2rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-avatar-lg` | `var(--ds-size-avatar-lg, 3rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-fund-bar` | `var(--ds-size-fund-bar, 1.4rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-stage-dot` | `var(--ds-size-stage-dot, 0.7rem)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-meter` | `var(--ds-size-meter, 6px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-swatch` | `var(--ds-size-swatch, 10px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--size-visually-hidden` | `var(--ds-size-visually-hidden, 1px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--effect-header-blur` | `var(--ds-blur-header, 8px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--transform-pressed` | `var(--ds-translate-pressed, 1px)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--background-none` | `var(--ds-background-none, none)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--background-app-texture` | `var(--ds-background-app-texture, none)` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--background-meter` | `var(--ds-background-meter, var(--color-interactive))` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |
| `--background-unknown-pattern` | `var(--ds-background-unknown-pattern, var(--color-neutral-soft))` | Use this project-semantic alias by meaning; never reach through to a `--ds-*` primitive from component CSS. |

## Consumption contract

1. Components in `global.css`, `components.css`, and feature CSS reference Layer 2 only.
2. Theme changes override Layer 1; aliases and components remain unchanged.
3. Add a primitive and a semantic alias together. Document the semantic meaning in the relevant foundation or component spec.
4. Run `node scripts/token-audit.js`; zero errors and zero warnings are required.
